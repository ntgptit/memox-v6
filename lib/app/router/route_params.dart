/// Canonical route parameter keys. The only place param keys may be
/// written (guard `memox.routing.no_raw_route_param_key`).
abstract final class RouteParams {
  static const String deckId = 'deckId';
}
