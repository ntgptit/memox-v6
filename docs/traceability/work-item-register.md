# Work-item register

- Status: **Current baseline**
- Owner: Delivery / QA
- Updated: 2026-07-25

## Prefix defaults

These defaults cover every WBS item. Item-specific rows below override them.

| Prefix | Owner | Domain | Canonical inputs | Default guard/test | Decision gate | Default status |
| --- | --- | --- | --- | --- | --- | --- |
| `P0.*` | Design System + QA + Flutter UI | Visual parity audit | Owner directive 2026-07-19, WBS §6.4–6.5 + Phase 0 register, kit shot matrix | Flutter Web + Playwright parity suite (≤3%) | Owner rule | Blocked |
| `MX-VIS-*` | Design System + QA | Per-state parity | Kit shot `<screen>--<state>--<theme>.png`, state fixture | Playwright spec + pixel diff ≤3%, both themes | Owner rule | Pending |
| `0.*` | Product + Architecture | Governance/decisions | `docs/architecture/**`, business/design owners | Docs links + decision/graph validation | Item override | Blocked |
| `1.*` | Platform + QA | Bootstrap/tooling | ADR-002, ADR-005, ADR-007, guard docs, Tier-1 resource budgets | Consolidated verifier contract tests | DG-02 | Blocked |
| `2.*` | Design System + Flutter UI | Theme/responsive | Design System v4 tokens, ADR-002 | Token/theme/responsive tests + design guard | DG-02, DG-05 | Blocked |
| `3.*` | Flutter UI + Accessibility | Shared `Mx*` | Frozen component docs, KIT-15..48 | Widget/golden/a11y + design guard | DG-02, DG-05 | Blocked |
| `4.*` | Data + Architecture | Persistence/DI | ADR-001, ADR-003, ADR-004, `docs/database/**` | DAO/repository/migration/provider tests | DG-01, DG-03, DG-06 | Blocked |
| `5.1.*` | Language Pair owner | Language Pair | `docs/business/language-pair/**`, language design specs | Domain/repo/provider/widget/E2E | DG-02, DG-05 | Blocked |
| `5.2.*` | Deck owner | Deck | ADR-001, `docs/business/deck/**`, Deck/Library design specs | Decision/transaction/widget/E2E | DG-01 | Blocked |
| `5.3.*` | Flashcard owner | Flashcard | `docs/business/flashcard/**`, editor/list design specs | Domain/repo/provider/widget/E2E | DG-01 | Blocked |
| `5.4.*` | Learning owner | Learning Progress/SRS | ADR-003, learning-progress specs | Policy/decision/property/transaction tests | DG-03, DG-04 | Blocked |
| `5.5.*` | Learning owner | Study Mode | ADR-003, study-mode specs | Shared mode/factory contract suite | DG-03 | **Implemented and verified; pending packet + DoD sign-off** — see `int-4` |
| `5.6.*` | Learning owner | Study Session | ADR-003, session/mode/result specs | Fake-clock/repo/provider/widget/restart E2E | DG-03 | Blocked |
| `5.7.*` | Today owner + Release QA | Today/first-learning release | All `5.1..5.6` gates, Today specs | Full Tier-1 journey + verifier marker | DG-01..DG-06 | Blocked |
| `6.*` | Content owners | Deck/Flashcard/Library | Owning business/design flows | Complete-slice gate | DG-01 | Blocked |
| `7.*` | Goal/Streak owners | Goal/Streak/Today/Result | Owning business/design flows | Clock/timezone/projection E2E | DG-02 | Blocked |
| `8.*` | Preferences owner | Preferences | Preferences specs + accepted scope | Provider/widget/persistence tests | DG-02 | Blocked |
| `9.*` | Reminder owner + Platform | Reminder | Reminder specs + Tier-1 capability matrix | Permission/time/platform E2E | DG-02 | Blocked |
| `10.*` | Search owner | Search | Search specs | Index/query/navigation E2E | DG-02 | Blocked |
| `11.*` | Statistics owner | Statistics | Statistics specs + source events | Projection/rebuild/performance tests | DG-03 | Blocked |
| `12.*` | Audio owner + Platform | Audio Playback | Audio specs + capability matrix | Lifecycle/platform E2E | DG-02 | Blocked |
| `13.*` | Transfer owner + Security | Content Transfer | Import/export specs | Parser/property/security/rollback tests | DG-06 | Blocked |
| `14.*` | Account owner + Security | Account/Sync | Account specs + accepted cloud-service gate | Sync chaos/security tests | DG-02, DG-06 | Blocked |
| `15.*` | Backup owner + Security | Backup | Backup specs + database migration contract | Disaster/rollback/compatibility tests | DG-06 | Blocked |
| `16.*` | Release QA | Release hardening | Release-scope milestone and all included wave gates | Full release verifier/certification | DG-01..DG-06 | Blocked |

## Item-specific overrides and evidence

Only rows below override the inherited status. `AC-WBS-*` and `TEST-WBS-*`
expand according to [the schema](./work-item-schema.md); Done evidence is durable
and item-specific, while Ready rows link exact execution packets.

| WBS | Status | Decision gate | Exact inputs | Acceptance/test | Packet | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| `P0.1` | **Done** (owner decision 2026-07-26) | Owner rule ≤3% | Implemented screens/dialogs/sheets in `lib/presentation/**`, kit shot matrix (480 PNGs) | `AC-WBS-P0-01`; `TEST-WBS-P0-01` | `docs/wbs/implementation-packets/WBS-P0-visual-parity-audit.md` | Census frozen. The five rows that waited on a kit decision (`003`, `006`, `007`, `008`, `024`) are resolved as **not measurable — no kit reference**, asserted by widget test instead of photographed, which is the precedent `MX-VIS-039`/`061` already set. Two of those assertions did not exist and were written before the rows were changed: `MX-VIS-006` (the sheet's unfiltered list — the filter test only proved what disappears) and `MX-VIS-024` (a failed root read must not render as "you have no decks"). `013`/`027` remain structurally unreachable. Every one of the 67 rows now resolves to a shot, a measurement, or a recorded reason, and `parity_census.mjs` enforces exactly that on every verifier run |
| `P0.2` | **Done** (2026-07-26) | Owner rule ≤3% | WBS §6.5 frozen environment | `AC-WBS-P0-02`; `TEST-WBS-P0-02` | Same packet | Harness delivered and its CI clause closed. `ci_scope.mjs` had emitted `visual_changes` since the verifier was built with no job consuming it; CI's `visual_parity` job now does, and `gate` requires it whenever that output is true — so the §6.5 rule is enforced by the build rather than by hand. Closed alongside `P0.6`, whose packet defines closure as flipping these rows. |
| `P0.3` | Done | Owner rule ≤3% | 1.6 deterministic ports, 1.7 dev fixtures | `AC-WBS-P0-03`; `TEST-WBS-P0-03` | Same packet | `lib/app/dev/parity_main.dart` (DB reset + reseed per load, `?fixture=` entry, default `MX-VIS-001`), `lib/app/dev/parity_overrides.dart` (per-fixture provider overrides), `lib/data/dev/parity_fixtures.dart`; built on the 1.6 injected clock/IDs/random and never wired into the release entry point |
| `P0.4` | **Done** (re-scoped by owner 2026-07-26) | Owner rule ≤3% | Phase 0 register, fixture layer | `AC-WBS-P0-04`; `TEST-WBS-P0-04` | Same packet | Completion criterion changed from "a measured ratio for every ID" to **"measured or explicitly accounted"**, because the literal reading could never be satisfied: states need features that do not exist (import, 13.x), shots the owner has superseded, or journeys §6.6 forbids. 34 of 67 measured; the other 33 each carry a recorded reason. The census already enforced this shape before the re-scope, so nothing loosened — the row now matches what the gate actually checks. **11 spec files** in `tool/parity/specs/`; live figures print on every verifier run rather than being frozen into this row |
| `P0.5` | **Done** (2026-07-26) | Owner rule ≤3% | Measured ratios from `P0.4` | `AC-WBS-P0-05`; `TEST-WBS-P0-04` | Same packet | Remediation complete for every state that can reach the gate: 64/68 state-theme runs pass. The 4 that do not are `MX-VIS-036`/`037` × 2 themes, now **permanent** recorded gaps rather than parked ones — the kit contradicts itself there, the owner ruled 2026-07-25 that the JSX wins and 2026-07-26 that the shot will not be redrawn. Each remaining divergence carries a concrete cause, never "minor rendering difference". |
| `P0.6` | **Done — Phase 0 exit gate closed** (owner 2026-07-26) | Owner rule ≤3% | All `P0.*` evidence, verifier contract | `AC-WBS-P0-06`; `TEST-WBS-P0-06` | Same packet | All three `AC-WBS-P0-06` clauses met. **(1) The verifier fails on regression:** `tool/parity/gate.mjs` is a ratchet that fails on any unrecorded failure *and* on any recorded gap that starts passing, run by `tool/parity/run.mjs` inside CI's `visual_parity` job. **(2) The merge gate is active:** `gate` requires that job whenever `scope` reports `visual_changes`, and rejects a parity job that ran when it should not have. **(3) New UI work is unblocked:** the 2026-07-19 STOP RULE is lifted as of this decision. **The §6.5 process rule stays in force** — no screen-changing PR merges without kit-parity evidence ≤3% — and is now machine-enforced rather than honoured by hand, which is the whole reason it is safe to lift the stop rule. Predecessors: `P0.1` frozen, `P0.4` re-scoped, `P0.5` carrying 4 permanent recorded gaps (`MX-VIS-036`/`037` × 2 themes) under the JSX-beats-shots ruling |
| `0.1` | Done | DG-01 | ADR-001, Deck business/design owners, mixed-content rows | `AC-WBS-0.1-01`; `TEST-WBS-0.1-01` | Not applicable — governance | `docs/architecture/adr/ADR-001-deck-content-model.md`; `docs/decision-tables/deck-mixed-content.md` |
| `0.2` | Done | DG-02, DG-05 | ADR-002, platform/localization matrices | `AC-WBS-0.2-01`; `TEST-WBS-0.2-01` | Not applicable — governance | `docs/architecture/adr/ADR-002-platform-and-adaptive-scope.md`; `docs/design/MemoX Design System_v4/SCOPE.md` |
| `0.3` | Done | DG-03, DG-04 | ADR-003, SRS policy, SRS8-001..028 | `AC-WBS-0.3-01`; `TEST-WBS-0.3-01` | Not applicable — governance | `docs/business/learning-progress/srs-8-box-policy.md`; `docs/decision-tables/srs-8-box-v1.md` |
| `0.4` | Done | DG-01, DG-04 | Guard memox ruleset and architecture paths | `AC-WBS-0.4-01`; `TEST-WBS-0.4-01` | Not applicable — governance | `docs/code-verification-guard.md`; guard regression suite in the consolidated verifier |
| `0.5` | Done | DG-01..DG-06 | Architecture, decisions, business, database, traceability | `AC-WBS-0.5-01`; `TEST-WBS-0.5-01` | Not applicable — governance | `docs/README.md`; docs inventory/link/traceability checks in the consolidated verifier |
| `0.6` | Done | DG-02, DG-05 | ADR-002 and release scope/exclusions | `AC-WBS-0.6-01`; `TEST-WBS-0.6-01` | Not applicable — governance | `docs/architecture/adr/ADR-002-platform-and-adaptive-scope.md`; `docs/design/MemoX Design System_v4/SCOPE.md` |
| `1.1` | Done | DG-02, DG-05, DG-06 | Current pubspec/lock, ADR-002/004/005/006/007, database and guard contracts | `AC-WBS-1.1-01`; `TEST-WBS-1.1-01` | `docs/wbs/implementation-packets/WBS-1.1-dependency-baseline.md` | `pubspec.yaml`/`pubspec.lock` (2026-07-19); full-gate pass marker `.dart_tool/memox_verify_pass.json`; `drift_dev` deferral evidence in the packet |
| `1.2` | Done | DG-02 | Verifier, hooks, CI and guard tests | `AC-WBS-1.2-01`; `TEST-WBS-1.2-01` | Not applicable — tooling already implemented | `tool/verify/run.mjs`; pass marker `.dart_tool/memox_verify_pass.json` emitted by the full gate |
| `4.1` | Done | DG-06 | ADR-004, Drift contract, unblocked drift_dev toolchain | `AC-WBS-4.1-01`; `TEST-WBS-4.1-01` | `docs/wbs/implementation-packets/WBS-4.1-database-runtime.md` | `core/database/database_opener.dart`, `data/database/app_database.dart` + 3 lifecycle tests (2026-07-19); web-wasm-assets boundary recorded (5.7.4); full-gate pass marker |
| `4.2` | Done | DG-06 | `docs/database/schema-v1.md`, ADR-004, SQL-first .drift directive | `AC-WBS-4.2-01`; `TEST-WBS-4.2-01` | `docs/wbs/implementation-packets/WBS-4.2-schema-v1.md` | All 17 tables in `data/database/tables/{content,progress,sessions}.drift` + 21 DDL contract tests, FK pragma in beforeOpen, `*_at` UTC naming convention (2026-07-19) |
| `4.3` | Done | DG-06 | `schema-v1.md` Deck exclusivity, deck business rules (organise/move) | `AC-WBS-4.3-01`; `TEST-WBS-4.3-01` | `docs/wbs/implementation-packets/WBS-4.3-schema-constraints.md` | Exclusivity + acyclic-tree triggers in `tables/constraints.drift` (`deck-mixed-content`/`deck-cycle` tags) + 7 contract tests incl. transactional rollback (2026-07-19); multi-isolate contention boundary → 5.7.4/16.1 |
| `4.4` | Done | DG-06 | WBS 4.4 DAO rules, SQL-first directive, business ownership map | `AC-WBS-4.4-01`; `TEST-WBS-4.4-01` | `docs/wbs/implementation-packets/WBS-4.4-dao-layer.md` | 11 DAOs over `queries/*.drift` (content, progress/attempts/preferences/goals/streaks, session/snapshot/checkpoint) + 20 DAO contract tests (2026-07-19) |
| `4.5` | Done | DG-06, ADR-005 | WBS 4.5 mapping rules, schema-v1 preference fallback | `AC-WBS-4.5-01`; `TEST-WBS-4.5-01` | `docs/wbs/implementation-packets/WBS-4.5-mappers.md` | Drift-free `lib/domain/**` models + `data/mappers/*` explicit toDomain with `DataCorruptionFailure` + preference null-fallback + 9 mapper tests (2026-07-19) |
| `4.6` | Done | DG-06, ADR-005 | WBS 4.6, schema-v1 atomic operations 1–7 | `AC-WBS-4.6-01`; `TEST-WBS-4.6-01` | `docs/wbs/implementation-packets/WBS-4.6-repositories.md` | 8 ports + Drift impls covering all seven schema-v1 atomic operations with idempotency keys, revision guards and rollback coverage; 18 repository contract tests (2026-07-19) |
| `4.7` | Done | DG-06, migration-policy | `migration-policy.md`, drift schema tooling | `AC-WBS-4.7-01`; `TEST-WBS-4.7-01` | `docs/wbs/implementation-packets/WBS-4.7-migrations.md` | `drift_schemas/drift_schema_v1.json` snapshot + generated verifier helpers + guided `app_migrations.dart` with post-upgrade FK integrity gate + snapshot/integrity tests (2026-07-19); drift pinned 2.34.0 for schema tooling |
| `4.8` | Done | Riverpod foundation, DG-06 | WBS 4.8, riverpod-foundation.md | `AC-WBS-4.8-01`; `TEST-WBS-4.8-01` | `docs/wbs/implementation-packets/WBS-4.8-di-graph.md` | `lib/app/di/data_providers.dart` (database + 8 port providers, keepAlive) + `di_graph.dart` fail-fast warm-up + 3 graph tests (2026-07-19); no DAO providers by design |
| `4.9` | Done | DG-06 | Schema-v1 indexes, 1.7 dense fixture | `AC-WBS-4.9-01`; `TEST-WBS-4.9-01` | `docs/wbs/implementation-packets/WBS-4.9-performance-baseline.md` | `QueryLimits` policy + 7 plan/limit tests (EXPLAIN pins 4 hot-path indexes; dense paging + stream bounds); latency/memory budgets recorded → device evidence at 5.7.4/16.1 (2026-07-19) |
| `4.10` | Done | Gate PASSED | All dependency rows Done; full verifier run | `AC-WBS-4.10-01`; `TEST-WBS-4.10-01` | `docs/wbs/implementation-packets/WBS-4.10-foundation-gate.md` | Foundation gate review 2026-07-19: 339 tests, guard 0 errors/0 memox warnings, all Accepted docs traced; platform boundaries carried to 5.7.4/16.1 |
| `5.1.1` | Done | DG-01 | language-pair business flows, validation catalog | `AC-WBS-5.1.1-01`; `TEST-WBS-5.1.1-01` | `docs/wbs/implementation-packets/WBS-5.1.1-language-pair-domain.md` | 3 use cases (create/select/remove) + normalized key + supported catalog + `ValidationFailure`; deck-dependency guard; 8 tests over real repos (2026-07-19) |
| `5.1.2` | Done | DG-01 | create-language-pair.md, 3.12 Mx set | `AC-WBS-5.1.2-01`; `TEST-WBS-5.1.2-01` | `docs/wbs/implementation-packets/WBS-5.1.2-first-run-language-ui.md` | First feature screen: route + draft/save viewmodels + searchable sheet + 5 widget tests over the real graph; first-run redirect boundary → 5.7 (2026-07-19) **Reopened 2026-07-20** for `MX-VIS-005`: the kit draws an inline error under the language select and `MxSelectRow` has no error slot, so the state is unreachable. **Re-closed 2026-07-21** (`637d97b`): step 1 gained per-field validation (`_meaningError`/`_RequiredSelectionError` in `first_run_language_screen.dart`, touched-on-dismiss semantics, `liveRegion` announcement); `MX-VIS-005` PASS 1.73% light / 1.72% dark. **Shared-layer debt recorded:** the fix renders the kit `field-group__error` line from a screen-private widget — `MxSelectRow` still has no `errorText`, while `MxFieldScaffold` already owns that line for boxed fields. Per FD-10 the slot belongs in the shared layer; consumer for the consolidation is `3.3`/`3.10` |
| `5.1.3` | Done | DG-01 | 5.1 coverage map | `AC-WBS-5.1.3-01`; `TEST-WBS-5.1.3-01` | `docs/wbs/implementation-packets/WBS-5.1.3-language-pair-evidence.md` | Provider/E2E/vi evidence suite + 4 committed light/dark×390/1024 goldens; on-device E2E boundary → 5.7.4 (2026-07-19). 5.1 block complete |
| `5.2.1` | Done | DG-01 | deck README §0, create/move-deck rules | `AC-WBS-5.2.1-01`; `TEST-WBS-5.2.1-01` | `docs/wbs/implementation-packets/WBS-5.2.1-deck-domain.md` | Derivation (empty/parent/leaf, mixed→corruption) + deck-name normalization + NEW `deck-pair-mismatch` triggers (cross-pair nesting gap closed) + contentCounts port; 6 tests; snapshot re-exported (2026-07-19) **Reopened 2026-07-20** for `MX-VIS-014`: no deck-name length rule exists anywhere in domain or presentation, so the too-long state cannot render. **Re-closed 2026-07-21**: `deckNameMaxLength = 60` with its reasoning at `lib/domain/deck/deck_name.dart:20`, enforced on the trimmed rune count; `MX-VIS-014` PASS 1.04% light / 1.07% dark. **Open decision:** no spec, schema or kit shot fixes the number — 60 is a chosen default and still needs the Deck owner's confirmation. |
| `5.2.2` | Done | DG-01 | create-deck.md, 5.2.1 invariants | `AC-WBS-5.2.2-01`; `TEST-WBS-5.2.2-01` | `docs/wbs/implementation-packets/WBS-5.2.2-create-deck.md` | CreateDeckUseCase: atomic insert, no auto content, retryDeckId idempotency, typed conflicts/validation + provider; 6 tests (2026-07-19) |
| `5.2.3` | Done | DG-01 | create-deck.md §§3–7, full-screen first-run rules | `AC-WBS-5.2.3-01`; `TEST-WBS-5.2.3-01` | `docs/wbs/implementation-packets/WBS-5.2.3-first-run-setup.md` | Full first-run journey: landing + Not-now + two-step setup + landing→deck E2E + vi + 8 goldens + MxButton label-overflow hardening (2026-07-19); success callout transfers to 5.2.4 (its real surface) **Reopened 2026-07-20** for `MX-VIS-015`: the draft persists but there is no resumed-vs-fresh flag, no `We kept your draft` banner and no `Start over` action, so the resume state cannot render. **Re-closed 2026-07-21**: `resumedDraft` flag + `draftKeptMessage` callout + `startOverLabel` discard action in `first_run_deck_setup_screen.dart`; `MX-VIS-015` PASS 0.73% light / 0.74% dark. First-run success now lands on the Library deck list per `create-deck.md` §7 (`4a84e48`), which retired the success callout and superseded `MX-VIS-021`/`022`. |
| `5.2.4` | Done | DG-01 | browse-nested/open-deck/create-deck §7–8 | `AC-WBS-5.2.4-01`; `TEST-WBS-5.2.4-01` | `docs/wbs/implementation-packets/WBS-5.2.4-library-open-deck.md` | Library root + §7 callout + Open Deck branching + §8 dialog (root/nested, pair inherited) + subtree card summary + dense/deep/long-name evidence; 13 widget tests across the block (2026-07-19) |
| `5.2.5` | Done | DG-01 | README §0, organise-deck.md | `AC-WBS-5.2.5-01`; `TEST-WBS-5.2.5-01` | `docs/wbs/implementation-packets/WBS-5.2.5-content-choice.md` | Decision-table suite (per-state action sets, reopen-on-Empty, store backstop) + Import tertiary on Empty; Add card→5.3, Import→8.x boundaries (2026-07-19) |
| `5.2.6` | Done | DG-01 | 5.2 coverage map | `AC-WBS-5.2.6-01`; `TEST-WBS-5.2.6-01` | `docs/wbs/implementation-packets/WBS-5.2.6-deck-evidence.md` | Provider/property/vi evidence + 4 goldens; coverage map across 30+ deck tests; on-device E2E → 5.7.4 (2026-07-19). 5.2 Deck block complete |
| `5.3.1` | Done | DG-01 | create/resolve-duplicate-flashcard.md, VAL-001 | `AC-WBS-5.3.1-01`; `TEST-WBS-5.3.1-01` | `docs/wbs/implementation-packets/WBS-5.3.1-flashcard-domain.md` | Child A: normalized_term column + duplicate-candidate query + CreateFlashcardUseCase (pre-commit candidates, keep-both, kept-id) + 7 tests (2026-07-19). Child B (`ce4e7a2`): Unicode NFC + case folding in `StringUtils` for VAL-001, `tag_label.dart` TAG-001..003, two-phase park/set translation reorder under the order-uniqueness constraint, transactional tag resolve/delete-unused, `ManageCard{Translations,Tags,Audio}UseCase` + DI; 16 tests (450 total). Child C (`d0898ff`): version-guarded `editCardContent` (stale-version conflict, never last-write-wins), `deleteCardCascade` tombstone in one transaction, `Edit`/`Hide`/`Delete`/`MoveFlashcardUseCase` with cross-pair and Parent-target rejection; 14 tests (464 total). 8 use cases in `lib/domain/usecases/flashcard/`; block complete 2026-07-19 |
| `5.3.2` | In progress (A Done) | DG-01 | create-flashcard.md, edit-flashcard.md, kit `flashcard-editor` (10 states) | `AC-WBS-5.3.2-01`; `TEST-WBS-5.3.2-01` | `docs/wbs/implementation-packets/WBS-5.3.2-card-editor.md` | Child A (`3ece904`): `card_editor_screen.dart` + `card_editor_viewmodel.dart`, `/deck/:id/new-card` route + `pushNewCard`, modal `MxContextualAppBar` variant, new shared `MxContextPill`/`MxFormFooter`, deck-driven language labels, tag labels resolved to ids before an atomic `CreateFlashcardUseCase` save, create-another toggle; the Empty-deck `Add card` CTA goes live, unblocking `empty-deck--default`. 4 widget tests (472 total) + parity for `flashcard-editor--create` and `empty-deck--default`; `MX-VIS-049` PASS 1.29% light / 1.61% dark. **Children B (duplicate resolution, submitting/error/success, dirty-discard) and C (edit mode, translations/audio sections, stale-target) pending** **Defect found and fixed 2026-07-25 (child B review):** the deck-detail *card-list* branch shipped `MxButton(label: addCard, onPressed: null)` behind the comment "Add card lands with the 5.3 flashcard flow" — a row that had already landed as child A. The empty-deck branch was wired to `pushNewCard`; the populated-leaf branch was not, so **a deck accepted its first card and no others** and the core loop was unreachable past one card per deck. Three widget tests asserted `find.text('Add card')` `findsOneWidget` and passed against the dead control the whole time; the leaf test now asserts `onPressed` is non-null. Audit of every remaining `onPressed: null` in `lib/presentation/features/`: three sites, all *Import cards*, all correctly disabled pending WBS 8.x with accurate comments. |
| `5.4.1` | Done | DG-03, DG-04 | initialise-card-progress.md §§1–5, §7; srs-8-box-policy.md Box 0; schema-v1 `learning_progress` | `AC-WBS-5.4.1-01`; `TEST-WBS-5.4.1-01` | `docs/wbs/implementation-packets/WBS-5.4.1-initial-progress.md` | `InitialiseCardProgressUseCase` implements the §3 Master flow: existing state returned untouched, unknown card rejected with `ValidationFailure(cardId, unknown)` **before** any write, and a stored card found without state repaired to New and audited via `AppLogger.warning`. New port `LearningProgressRepository.initialiseNew` reads back inside the same transaction as `insertNewProgressIfAbsent` (`INSERT OR IGNORE`, `box = 0`/`due_at = NULL` pinned in SQL so no call site can persist a New row that already activated SRS; the `card_id` UNIQUE index is what collapses a concurrent initialise to one row). 6 tests over the real Drift graph — New contract field by field incl. `policyId = leitner-8-box-v1` and no Attempt, create-path read, repeat initialise, learned card keeping box/due/counters **and revision**, unknown card leaving zero rows, and 3 concurrent calls resolving to one id (2026-07-25). Import/restore entry points (§2 rows 2–4) stay with `13.1`/`15.3`; the `int-2` counter query is `5.4.2` |
| `5.4.2` | Done | DG-03, DG-04 | surface-due-cards.md §§1,2,4,5,7,9; schema-v1 box CHECK; the 5.4.1 guarantee that every card carries a progress row | `AC-WBS-5.4.2-01`; `TEST-WBS-5.4.2-01` | `docs/wbs/implementation-packets/WBS-5.4.2-due-new-queues.md` | Classification moved onto the box per §4 (New = Box 0, Due = Box 1..7 reached, Box 8 in no queue) in `countDeckQueues` (recursive subtree, so Leaf/Parent/Empty all fall out of one walk), `countLibraryQueues`, and `pageDueProgress`/`countDueProgress`. `StudyQueueCounts` separates `hasNoEligibleCards` from `hasNothingDue` (§9). `LoadStudyQueueCountsUseCase` exposes `forDeck`/`forLibrary` with a typed unknown-deck failure. **Closes `int-2`** — `watchRootDeckSummaries` now reads New as Box 0 instead of a missing progress row. 9 new tests + the Library summary test rewritten to build cards through `CreateFlashcardUseCase` (its old fixture asserted a no-progress-row card, which is what hid the defect) (2026-07-25). Relearn queue → `5.6.13` (needs finalized-session history); new-card limits/order → `8.2`+`7.1`; Library subtree display → Library display contract with its `MX-VIS-020` evidence |
| `5.4.3` | Done | DG-03, DG-04 | srs-8-box-policy.md §§1–6, 8, 12; decision table SRS8-001..028; guard `memox.study.srs_logic_single_source` | `AC-WBS-5.4.3-01`; `TEST-WBS-5.4.3-01` | `docs/wbs/implementation-packets/WBS-5.4.3-srs-8-box-policy.md` | `lib/domain/learning_progress/srs_8_box_policy.dart` — the single pure-domain source of the transition math, per the error-severity guard that pins `intervalForBox`/`boxAfterFinalization` to this path. Fixed 1/3/7/14/30/60/120 ladder; promote capped at 8, demote floored at 1, never back to Box 0; Box 8 schedules `null`. `terminalGrade` folds a session's committed evidence with wrong sticky across retry rounds (`SRS8-010`) and **returns `null` for empty evidence** so a skipped card under snapshot recovery is never scheduled as a free `correct` (§4). Foreign `policyId` fails typed (`SRS8-028`). 37 tests naming their row IDs, with an exhaustiveness assertion over the `(box, grade)` space (2026-07-25). **Finding: the decision table is not exhaustive** — Box 7 + `wrong` has no row though the header claims completeness; §5 settles it (Box 6, +60d) and the suite covers it as derived, but ID allocation is the table owner's call. Recorded under *Coverage gap* in `docs/decision-tables/srs-8-box-v1.md`. Persistence/idempotency/stale-writer (`SRS8-011`/`-012`) → `5.4.4`, which also owns adding the `srsActivatedAt`/`lastReviewedAt` columns `SrsSchedule` already returns |
| `int-1` | Done | — | User request: make built flows reachable for incremental testing | — | (integration commit) | First-run redirect gate (pulled forward from the 5.7 navigation guard) + Library entry on the home placeholder + 3 redirect tests; foundation goldens regenerated (2026-07-19) |
| `5.4.4` | **Done** (2026-07-25) | DG-03, DG-04 | srs-8-box-policy.md §§3, 8; migration-policy.md rules 1 and 6; schema doc SRS columns | `AC-WBS-5.4.4-01`; `TEST-WBS-5.4.4-01` | `docs/wbs/implementation-packets/WBS-5.4.4-attempt-schedule-transaction.md` | Schema **v2** — the repo's first real migration; v1 was create-only. `learning_progress` gains `srs_activated_at`/`last_reviewed_at`, which `SrsSchedule` had returned since `5.4.3` with nowhere to go: `ApplyTerminalOutcomeUseCase._persist` dropped both and said so in a comment, so every terminal grade recomputed and discarded a card's activation and last-review instants. Both now travel entity → port → repository → guarded DAO update, inside the existing revision check and transaction. **Defect fixed in the same change:** `Srs8BoxPolicy.applyTerminalGrade` took `srsActivatedAt` as an *optional* parameter defaulting to `null` that no caller in `lib/` or `test/` ever passed — inert only while nothing persisted it, and the moment v2 stored the value it would have written NULL over a real activation instant on **every grade**, silently resetting the one field §3 says never moves. The parameter existed because the entity had no field to read; it is removed and the policy reads `current.srsActivatedAt`. **The migration does not backfill:** v1 rows were activated at an instant no column recorded, and `updated_at` is the last review rather than the first activation, so writing it would be fabrication — rule 6 forbids it. NULL is disambiguated by `box` (0 = never activated, >= 1 = pre-v2, unknown). **Child B:** the pre-existing conflict test fed an impossible `expectedRevision: 9`, proving the guard rejects a bad argument rather than that it fires when it must; the new test has both writers read the card's actual revision and race, so exactly one commits, the other raises `ConflictFailure`, and the loser leaves no evidence row. **Child C:** in one connection a deduped replay is indistinguishable from an in-memory cache doing the work, so the file-backed restart harness reopens the store with a new connection and repository — a replayed key does not re-grade (asserted against a deliberately *different* schedule so a failure would show), and a new key still applies, so dedupe has not become blanket rejection. **Second defect fixed in review:** `applyScheduledOutcome` and `saveAttemptWithCheckpoint` treated any stored idempotency key as a replay and returned success. The key column is *globally* unique, so a key reused across cards would drop a grade with no write and no error while the caller believed it succeeded. Unreachable today (keys are card-scoped by construction), both paths now check the stored attempt's `cardId` and raise `ValidationFailure(idempotencyKey, 'card-mismatch')` |
| `5.4.5` | **Done** (2026-07-25) | DG-03, DG-04 | surface-due-cards.md §§1, 7, 9; srs-8-box-policy.md §6 | `AC-WBS-5.4.5-01`; `TEST-WBS-5.4.5-01` | `docs/wbs/implementation-packets/WBS-5.4.5-progress-tests.md` | Audited first: five of the row's seven coverage categories were already satisfied by the rows that built the behaviour — the decision table (all **28** `SRS8-*` rows named, with an exhaustiveness assertion), property, idempotency, repository and migration. Two were not. **Boundary** asserted only `due_at == nowUtc`; the comparison is `<=`, so the two instants either side of equality are the interesting part and are now pinned to the millisecond. **Timezone** existed only as a doc comment on `LoadStudyQueueCountsUseCase`: §9 requires that due eligibility be a UTC instant comparison and that a timezone change not move a persisted `dueAt`, and nothing held either half. Dart cannot repoint the process timezone mid-test, so the same instant is expressed at a different offset (`19:00+07:00` vs the fixture's `12:00Z`) — a wall-clock comparison anywhere in the chain would make the two readings disagree. Also pinned the clock port: the existing `withClock` test fixed a zone that was already UTC, so it passed whether or not `nowUtc()` converted; the new one uses a non-UTC zone and asserts the instant survives, **verified by mutation** (removing `.toUtc()` fails it, restoring passes). No defect found — the port, the queue comparison and the policy arithmetic all behaved as specified |
| `int-10` | **Open — P0 status re-audited** (2026-07-26) | DG-01 | `AC-WBS-P0-06` clauses; `WBS-P0-visual-parity-audit.md` child boundaries | — | (audit, no PR) | **The register was telling the owner the wrong reason the Phase 0 exit gate is stuck.** `P0.6` read "cannot close while its first DoD clause is unmet" — the parity suite running in the verifier and failing on regression. That has been true since 2026-07-25 (ratchet + required CI job). Meanwhile `P0.4` still claimed "4 app-launch specs covering 11 of 49 IDs" against an actual 11 spec files and 67 rows / 32 measured. Both rows are corrected and `P0.6` now names its real blockers: five kit-owner census decisions, coverage at 32 of 67, and the two owner-parked shot contradictions. **Why it matters:** `P0.6` blocks every remaining UI work package (`5.3.2+`, `5.6.*`, `5.7.*`, `6.*`–`16.*`), so a wrong blocker on that row is the most expensive stale sentence in the repo — it points the owner at CI work that is already done instead of at the decisions only they can make. Same drift class the recorded-percentage check now guards, one level up: prose claims about coverage rot silently while the numbers move |
| `5.7.3` | **Done** (2026-07-27) | DG-03, DG-04 | start-review-from-today.md §§1-2, 4, 6; continue-session-from-today.md §§1-2, 4, 6 | `TEST-WBS-5.7.3-01` | PRs #181, #182, #193 | Both Today handoffs revalidate before they navigate; each used to open its route on a projection composed when the screen loaded. **Start review** (#181): an active session outranks a new review; a stale count resolves to caught-up rather than an error; one eligible deck starts a session over its subtree. **Several eligible decks now ask which** (#193). That branch opened the Library until then — neither resolving the scope nor handing off, just leaving the learner to find a deck themselves and discarding the counts the revalidation had already read. The picker carries those counts, and the chosen deck is revalidated on the way through: a queue can empty while a sheet sits open, and §6 forbids a stale count from creating an empty session. A library-wide session remains unrepresentable in schema v1 (`study_sessions.deck_id` NOT NULL, `scope` CHECKed to leaf/subtree), which is why the scope must be chosen at all. **Resume session** (#182): the session is resolved from the store again; if it was finalized elsewhere the projection refreshes *and* the reason is stated. It creates nothing (§6). Both lock their CTA while in flight, with the guard in the command rather than the button, and both keep the dashboard and report in place on failure |
| `5.7.1` | **Done** (2026-07-27) | DG-03 | load-today-dashboard.md §§1-3; surface-due-cards.md §1; srs-8-box-policy.md §3 | `TEST-WBS-5.7.1-01` | PRs #176-#201 | The Today read projection composes the session, due and new queues, the goal and streak standing, the library's mastered share and the recent decks — each pulled from its owning source, none recomputed here. Every seam into it is now driven end to end rather than asserted per side: study (`int-24`), library edits (`int-25`, `int-26`), and the active language pair (`int-27`). Four of this session's defects lived on exactly those boundaries with both sides individually correct |
| `5.7.2` | **Done** (2026-07-27) | DG-01, DG-03 | kit `dashboard`; the seven `today-dashboard/**` flows | `TEST-WBS-5.7.2-01`; `MX-VIS-068`-`076` | PRs #176-#199 | Every section of the kit composition is built and in the kit's order: greeting, state note, Continue-studying, Daily-goal card, stat strip, Recent decks and the create FAB — plus the loading skeleton, the load-error retry and the stale-refresh marker. All seven flow documents under `today-dashboard/**` have been implemented and read against their specs. `MX-VIS-069` is **measured** at 7.67% light / 9.45% dark and recorded as a gap; its residual is enumerated on the row and none of it is unbuilt composition |
| `5.7.4` | **Partial** (2026-07-27) | DG-03, DG-04 | WBS 5.7.4 scope line; srs-8-box-policy.md SRS8-001 | `TEST-WBS-5.7.4-release`; `TEST-WBS-5.7.4-contrib` | PRs #194, #195 | The release journey is covered at the domain level over the real store: fresh install → five valid cards → newLearning through all five stages → finalize → the Today read that follows, then the same cards returning as due once their dates pass (`int-22`). The contribution round trip — what finalize writes is what Today reads — is covered too, and found `int-23`. Force-close/resume is covered by `int-20` over a real process restart. **Outstanding:** the row also asks for offline Web and Android variants of the journey. Neither exists, and neither is a domain test — they need the Tier-1 harness `5.7.5` depends on |
| `5.6.14` | **Covered — promotion is the owner's** (2026-07-27) | DG-03 | WBS 5.6.14 scope line; §6.2 packet rule; §6.5 parity rule | see the layers below | PRs #202-#222 | The row asks for six layers and three named rows. All nine now exist, and this is where they are. **Contract:** `test/domain/study_modes/mode_contract_test.dart` over every mode. **Repository:** `test/data/repositories/session_repository_test.dart`, plus `srs_exactly_once_restart_test.dart`. **Provider:** `study_session_runtime_provider_test.dart` and the notifier tests under `presentation/features/study/viewmodels/**`. **Fake clock:** `test/support/fake_clock.dart`, pinned into the session tests. **Widget:** all six mode screens, the shell, the dispatcher and the result. **Interruption/restart E2E:** `resume_after_restart_test.dart` over a real file-backed store through `RestartHarness`, joined by `recall_timer_survives_exit_test.dart`. The three named rows: *durable answer* — `answer_save_failure_test.dart` and the resume pipeline; *Recall 20s* — the deadline case in `recall_screen_test.dart` and the countdown that now survives an exit; *exactly-once terminal scheduling* — `srs_exactly_once_restart_test.dart`. **One substitution, stated rather than glossed:** there are no Flutter goldens for the study screens. Their visual authority is the kit-parity harness and the `MX-VIS-*` rows, which §6.5 makes the rule for any screen-changing PR; goldens are kept for the foundation, deck and language-pair surfaces where no kit shot governs. **What assembling this found:** every layer was present and every layer passed while `int-44` made it impossible to finish a session at all. The layers tested the pieces; none of them drove the path the app takes, which goes back to the store between answers. That is why `int-45` and `int-46` were added on top, and why they belong on this row rather than beside it. Coverage is therefore complete. The row stays un-promoted because §6.2 requires an item packet first and the Done/packet call is the owner's — the same standing as `5.5`, and a process gap rather than a coverage one |
| `5.7.5` | **Blocked — two blockers, and one of them is now unsatisfiable as written** (2026-07-27) | DG-01, DG-03 | WBS 5.7.5 criteria; §6.5 parity rule; owner rulings 2026-07-25 / 2026-07-26 | — | (audit, no PR) | **The release gate had no status row at all**, so nothing told the owner where it stood — the same defect `int-10` recorded one level up. Its four criteria, re-audited after the 5.6/5.7 spec sweep: **(1) *consolidated verifier passes* — yes.** 20/20 steps, 1236 tests (1196 when this row was last updated, 1136 when it was written). **(2) *study test pyramid complete* — yes**, audited layer by layer on the `5.6.14` row above, and since strengthened where the sweep found it thin: `int-44` added an end-to-end layer after every existing layer passed while no session could be finished, and `int-78`/`int-81` added a retry-round layer after an element-lifetime defect walked through every per-mode test. `5.6.14` stays un-promoted for want of an item packet (§6.2) — a process gap, not a coverage one. **(3) *canonical parity evidence* — no, and this row previously understated it.** It named `MX-VIS-069` alone; the census fails **five** states, ten runs: `069` at 7.67/9.45, `036` at 12.11/18.15, `037` at 8.12/9.70, `078` at 24.88/25.29, `079` at 28.59/28.96. They have two causes, and neither is unbuilt composition. `069` is Today, whose residual is two stat cells with no producer (`int-12`) and the app bar's bell and avatar, which need features WBS 14.x owns. The other four are the **same kit self-contradiction** (`int-56`): the shots draw chrome the kit's own JSX does not render. The owner ruled on 2026-07-25 that the JSX is the standard, and on 2026-07-26 that the shots will not be regenerated. Those two rulings together mean these four states **cannot reach ≤3% against the shots they are measured on** — not "not yet", but not ever, while both rulings stand. So criterion (3) as written is unsatisfiable, and the gate needs the owner to either exempt states whose shots the same owner has declared stale, or reverse the regeneration ruling. Recording it as merely "blocked" invited the reading that someone could finish it. **(4) *Tier-1 Web/Android smoke evidence* — half of it now exists.** The **Web** half is `tool/smoke/web.mjs`, and it passes: a release build of the *shipping* entry (`lib/main.dart`, not the parity entry) served and loaded in Chromium at 390x780, reaching the first-run landing with no console or page errors. It writes `evidence/smoke/web/` — a screenshot and a result record — which stays untracked like the parity captures beside it; the harness is the committed artefact and re-running it reproduces the evidence. That distinction is the point of it — every existing Web check drives `lib/app/dev/parity_main.dart`, a debug entry that seeds fixtures and installs overrides, so **nothing had ever checked that the shipping entry boots on Web at all**. It does. **Wired into CI in PR #251**, so it is a gate rather than a thing someone remembers to run: a `web_smoke` job mirroring `visual_parity`, skipped only for docs-only PRs, uploading its screenshots as artifacts, and required by the `gate` job — where a *skipped* smoke on an app-touching PR counts as a failure, so the job cannot be quietly disabled. The gate's branch logic was extracted and exercised over all four outcomes; the job itself runs green only in CI, which is not verifiable from this environment and is not claimed to be. The **Android** half does not exist and is not pretended to: it needs a device or emulator this environment has no access to, so criterion (4) stays open on that half alone. A machine check for (4) was attempted earlier and **withdrawn**: matching the phrase fired on the WBS line stating the criterion and on this row reporting it missing, and the status-based rewrite could not be shown to fire. So the gate is blocked on (3) and on the Android half of (4), and on nothing else. Both need the owner: (3) needs a ruling that reconciles the parity bar with the shot-regeneration ruling, and (4) needs an Android runner, which no row owns and no environment here provides |
| `int-31` | **Fixed** (2026-07-27) | DG-03 | reset-deck-progress.md §3, §5, §7, §11; srs-8-box-policy.md | `TEST-WBS-6.1-reset-impact` | PR #205 | **The reset confirm named every card in scope, not the ones it would actually reset.** §5 is explicit: "Affected count chỉ gồm cards có progress cần reset." The dialog read `cardCount` — the whole subtree — so a deck of forty cards with three studied said *"This will reset progress for 40 cards"*, promising to undo work nobody had done. The same wrong number decided the state: `nothingToReset` was `cardCount == 0`, while §3 branches on "Has progress?". A deck of never-introduced cards therefore skipped the nothing-to-reset state (§7, §11) and offered the destructive action, which would have run a transaction that changed nothing. Both now read `studiedCardCount` — the count `int-30` added for the delete confirm, and the same quantity by construction: the cards a reset returns to the unlearned state are exactly the cards that left it. Four seam tests run the summary and the reset over one store, including the Box 8 case a due-only reading would miss — a mastered card sits in no queue until the reset puts it back in the new one |
| `int-32` | **Fixed** (2026-07-27) | DG-03 | reset-deck-progress.md §5, §10, §12 | `TEST-WBS-6.1-reset-availability` | PR #206 | **A reset while a session was running silently rewrote the queue that session was reading.** §5: "Active session không bị silently rewrite: chặn reset hoặc yêu cầu kết thúc session trước"; §10 lists `active-session blocked` as a state and §12 makes it an acceptance criterion. Nothing in the reset path consulted the session repository, so a reset landed underneath a live session whose cards were frozen at start — the session then finalized against SRS state that had moved beneath it, with nothing said to the learner. §5 offers two remedies and this takes the first: the reset is refused. The overlap test needed no new repository surface. A reset covers a subtree; a session covers its own deck (`leaf`) or its subtree (`subtree`); the two intersect exactly when one deck is the other's ancestor-or-self, which the existing `ancestors()` port answers. The asymmetry is the part worth having in a test: a session *below* the reset scope blocks it whatever its own scope, while a session *above* it only blocks when that session is itself a subtree one. A session on an unrelated deck blocks nothing — a guard that held the whole library hostage would be its own defect. Both sides are guarded: the dialog reads availability and shows the blocked state instead of an action that would be refused, and the command re-checks, because a session can start while the dialog is open. Eight tests, including the command refusing with `ConflictFailure(code: 'session-active')` and leaving every box where it was |
| `int-35` | **Fixed** (2026-07-27) | DG-03 | hide-flashcard.md §7, §9; delete-flashcard.md §7 | `TEST-WBS-6.5-card-outcome` | PR #208 | **A hide or delete that failed reached nobody.** `CardLifecycleCommandViewmodel` is documented as kept alive *because* nothing watches it — the caller only `read`s it. That is true, and it meant the `AsyncError` those commands assign to their own state had no reader: a failed hide left the row exactly as it was, which is also what a successful hide of an already-hidden card looks like. The sheet has closed by then, so there was no inline surface left to report into either. Each command now returns the value it assigns to state, and the dispatcher reports it — the spec's own copy ("Couldn't update this card. Try again.", "Couldn't delete the card. Nothing has been removed.") in an error-tone bar. The state assignment stays; the return is the same value, for the caller standing right there. Proven by deleting the card row while its sheet is open — `hide-flashcard.md` §9's concurrent case, and the one way this fails for real |
| `int-37` | **Fixed** (2026-07-27) | DG-03 | set-daily-study-goal.md §6, §8 | `TEST-WBS-goal-save-failure` | PR #209 | **A daily goal that never reached storage closed the sheet and read as saved.** §6: "Failure: Couldn't update your daily goal. Your changes are still here." The sheet awaited the command and then popped unconditionally, so the only difference between a saved goal and a lost one was that the dashboard never showed it. The learner would go looking. The sheet now closes on success only — with the `Daily goal updated` confirmation §6 asks for — and a failure keeps the sheet, the draft target and an inline error where the action was. Fixing it turned up a second half of the same defect one layer down: the command invalidated the goal read *whatever* happened. A failed write leaves the stored goal alone, so there is nothing new to read — and re-reading through a store that has just failed swaps the open form for its error state, taking the very draft §6 promises to keep. The invalidate is now on success only, which is also what makes the failure honestly testable: the store is closed under the open sheet, and what stays on screen is the draft |
| `int-42` | **Fixed** (2026-07-27) | DG-03 | ST-CONTENT-CHANGE-v1 ST-CHG-007; delete-flashcard.md §5, §8 | `TEST-WBS-6.5-delete-blocked` | PR #214 | **The card on screen could be deleted out from under the learner mid-answer.** ST-CHG-007 and §5 both say block it: "Current prompt hoặc pending answer | Block delete; user phải exit, commit answer hoặc explicit skip trước delete." Nothing in the delete path knew a session existed. The row *above* it in the same table allows deleting a card that is in the session but further down the queue, so the rule is narrow, and the test asserts both halves — a guard that also stopped the permitted delete would be reading "in a session" where the table says "is the prompt". A session whose runtime cannot be assembled raises rather than resolving to "no current prompt": that is exactly the case where nobody can say whether this card is the one on screen, and a delete that cannot be shown to be safe is not one to wave through. The refusal reaches the learner as its own message rather than the generic delete failure — being told "nothing has been removed, try again" would send them back to do the same thing |
| `int-82` | **Fixed** (2026-07-27) | DG-03 | exit-study-session.md §5 ("Waiting/typing, not submitted → Warn unfinished input not saved"), §9; match-terms-and-meanings.md §4 | `TEST-WBS-5.6.6-exit-warning` | PR #244 | **Leaving a Match round warned about the wrong thing, by an entire board.** §4's exit copy names one thing that may be lost — \"the current unfinished answer\" — which is true of every stage that answers a card at a time. Match is not one of those: a round commits nothing until the whole board is cleared, so a learner nine pairs into ten has nine unsaved matches, and the confirm told them one answer might need re-entering. The shell now takes a per-stage note and Match supplies one whenever a pair is locked, saying plainly that the round starts over |
| `int-96` | **Fixed** (2026-07-28) | FC-04, DG-01 | resolve-duplicate-flashcard.md §2, §5, §6, §8 | `TEST-WBS-5.3.2B-duplicate-review`; `MX-VIS-055` 2.16% light / 2.40% dark | PR #267 | **“View existing” destroyed the draft.** §5 is explicit — “Open existing: draft retained until explicit discard/return” — and §8 repeats it as an acceptance criterion. The button called `go` to the existing card's *deck*, and `go` replaces the route stack: the editor and everything typed into it were gone, with no Back to bring them back. Inspecting the card you are being warned about is not a decision to abandon your work. It now pushes the existing **card**, which is what §2 and §3 mean by *open existing*, and Back returns to the draft intact. **Second defect:** the banner said a card “already exists **in this deck**” while detection runs across the whole language pair. A learner told that would go looking in a deck the card need not be in. **Third:** §6 gives the duplicate *lookup* its own failure line, and a lookup that could not run reported as a save that was rejected — different places to be, the same sentence. The lookup is tagged `duplicate-check-failed` in both the create and the edit path. The parity spec asserted the old copy, so `MX-VIS-055` broke on the assertion before it ever reached a pixel; re-measured after fixing it, the state sits at 2.16/2.40 against a 2.07/2.31 baseline — the longer sentence, and inside the bar. **Split out to `int-97`:** §4's comparison block, which was built and measured and does not fit under the §6.5 bar. **Not built:** §1's exact-vs-possible distinction (detection is exact normalized-term equality, so every candidate is exact) and §5's merge, which needs the two-card merge target flow no row owns |
| `int-100` | **Fixed** (2026-07-28) | FC-07, SR-02 | view-card-detail.md; open-search-result.md §3; hide-flashcard.md §2; move-flashcard.md §2 | `TEST-WBS-6.x-card-detail` | PR #271 | **Card Detail did not exist, and three flows were quietly routing around the hole.** `open-search-result.md` §3 branches “Valid Deck → Open Deck contract” and “Valid Card → Open Card/detail contract”; search opened the **editor** instead, with a code comment claiming the spec said so. It does not. Looking something up dropped the learner into a form over content they had only asked to read. `hide-flashcard.md` §2 (“Card action sheet/detail”) and `move-flashcard.md` §2 (“Card detail → Move”) both name it as an entry point too. The screen is the read projection its spec describes and nothing more: term, primary meaning, additional translations, tags, the owning deck and a read-only Box/due summary. It owns no rules — the one action opens the same settings sheet the Leaf list uses, and hide and delete hand the learner back to the deck that owns those flows end to end rather than growing a second, thinner copy of the same confirms. Loading, loaded, not-found and error are all covered; a card deleted from another screen resolves to not-found with a return to its deck, and a test taps that return because a dead end whose only way out is broken is the worst version of this screen. **Parity:** Card Detail has **no kit reference and no `MX-VIS` row**, like the Search screen. It cannot be measured, so §6.5's bar does not apply and no shot was regressed; it needs a register row and a kit decision before it can be. Recorded rather than assumed. **A gap in the local gate, found by it failing in CI instead.** `run.mjs`'s format check listed files with `git ls-files '*.dart'` — the index only — so both new files here were invisible to it until the commit made them tracked, and CI saw the misformatting the local run had just passed. New files are exactly when formatting is most likely to be wrong. It now lists `--cached --others --exclude-standard`, verified by checking an untracked probe appears. Same family as the recorded `--quick` gap. **Not built:** `open-search-result.md` §3's stale branch — a result whose card was deleted or hidden since the search ran should be removed from the list with guidance, not opened. Card Detail now shows not-found rather than crashing, which is the safe half; the list-side cleanup is its own round |
| `int-99` | **Fixed** (2026-07-28) | FC-06, DG-01 | manage-card-translations.md §1, §3, §6, §9 | `TEST-WBS-6.4-translation-draft`; `MX-VIS-060` 1.49% light / 2.61% dark | PR #270 | **A translation removed by mistake was gone before Save, and Discard restored nothing.** §6 is explicit twice over — “Child edits live in parent Card draft until Save” and “Removing existing translation can be undone by discard parent draft” — and §3 ends the flow at “Save Card atomically”. The edit-mode section wrote every add and remove straight through to the store the moment it was tapped; its own doc comment said so. **The create path had always been right**, collecting translations and committing them inside the card's atomic operation, so the two halves of one flow disagreed. **The quieter half of the defect:** because the removal committed immediately, nothing marked the editor dirty — so Cancel closed without asking, and the guard that exists to stop work being thrown away could not see the work being thrown away. Edit now carries the draft: `editCardContent` takes the desired translations and applies them in the same transaction as the card's own fields, deleting what the draft dropped, updating what it kept **by id** (§1 asks for stable identity, so an edited row is not deleted and recreated), and assigning contiguous order through the same park-then-set phases the standalone reorder uses. Duplicates validate against the primary meaning *being saved* rather than the stored one, because both change in the same commit. **Not built:** §4's per-row numbering and `More` menu, and §5/§9's accessible reorder — there is no move action at all today, so order is whatever insertion gave it. §5's “Use a shorter translation.” has no bound to enforce: `validateCardText` rejects blank and nothing else, and the spec names no limit |
| `int-98` | **Fixed** (2026-07-28) | SR-01, SR-02 | search/README.md `search-rank-v1`; filter-search-results.md §1, §4, §6; hide-flashcard.md §1 | `TEST-WBS-10.2-search-visibility` | PR #269 | **A correction to `int-93`, and the missing half of it.** `int-93` made hidden cards findable, which “hide-flashcard.md” §1 requires, and I recorded it as uncontested. It was not: `search-rank-v1` says “Hidden/deleted content bị loại trước ranking” and I had not read it. The two reconcile, and the next sentence of the policy is how — “Filters chạy trước ranking”. That clause is the *default visibility filter*, not a rule that hidden content is unsearchable, and it cannot be the latter: `filter-search-results.md` §1 lists visibility as one of four filter dimensions, and a visibility filter over a set that never contains hidden cards would filter nothing. So `int-93` was half right. Hidden cards belong in the searchable set; showing them unconditionally dropped the default the policy states. Search now excludes them by default and a `Hidden` chip brings them back — a toggle rather than a fourth type, because a learner can want cards *and* want the hidden ones. **Two consequences worth their own lines.** The filtered-empty state said “No results of this type”, which names the wrong filter once visibility is one; it now names neither. And `Clear filters` had to clear *both*, or a query whose only matches were hidden would clear the type chip, stay empty, and leave the learner pressing a button that does nothing — §4 asks for a way out, not a label. **Not built:** tags are not searchable. `manage-card-tags.md` opens by calling them labels “for organization and search”, but `search-rank-v1`'s field priority is `Card.term` → `Card.primaryMeaning` → `Deck.name` → accepted translation, with no tag tier — so tag search needs a ranking-policy decision before it can be built, not just a query |
| `int-97` | **Owner decision — two of them, one urgent** (2026-07-28) | DG-01, DG-03 | WBS §6.5 process rule; resolve-duplicate-flashcard.md §4; `FlashcardEditor.jsx` header CF-07; `tool/verify/ci_scope.mjs` | `MX-VIS-055` measured 2.99% light / 3.40% dark with the block | (measured, not merged) | **(1) The §6.5 parity rule does not fire for the commonest screen change: editing a screen.** The WBS records the rule as machine-enforced — “CI's `visual_parity` job runs the ratchet and `gate` requires it whenever `scope` reports `visual_changes`” — and `gate` does exactly that. But `ci_scope.mjs` sets `visualChanges` in only two places: a changed non-`.md` file under `docs/`, and a changed *golden or parity test file*. A PR that edits `lib/presentation/features/**` — a screen — never sets it, so `visual_parity` skips and `gate` is satisfied. Every screen-changing PR this session merged with the job skipped, including this one. It was caught only by running the row by hand. **The obvious fix is blocked by `int-87`:** CI checkouts hold 12 of 219 kit shots (the rest are gitignored at 36 MB), and `tool/parity/preflight.mjs` refuses the suite without them. Flipping the flag would turn every screen PR red rather than enforce anything. So this needs the shots in CI — LFS, a release artifact, or a fetch step — before the rule can mean what the WBS says it means. **(2) §4's duplicate comparison cannot reach ≤3% against a shot that cannot contain it.** §4 draws the decision as a banner *plus* a detail block: the draft beside the stored card, term and meaning each, under “Review the differences before saving.” Without it the review names the term — the one thing the two cards are guaranteed to share — so `Add anyway` and `View existing` are both guesses. Built and measured, it takes `MX-VIS-055` from 2.16/2.40 to **2.99 light / 3.40 dark**, and dark fails the bar. This is not a fidelity defect: the kit's own header says so, in `FlashcardEditor.jsx` line 11 — “CF-07 duplicate: business is a 4-way Edit / Open-existing / Keep-both / Merge with compare” — and the parity spec repeats it. The kit **defers** the surface to implementation, so its shot is incomplete by design and every pixel the deferred surface occupies counts against the row. Same family as `int-56`, with a different cause: not a shot that contradicts the JSX, but a shot the JSX never claimed to finish. The owner's call is whether to exempt `MX-VIS-055` on that basis (the block then lands as written), re-shoot it with the compare surface, or leave the learner deciding on a term alone. The code is not in the branch; the measurement is |
| `int-95` | **Fixed** (2026-07-28) | DG-03 | WBS 5.7.4 ("offline Web ... variants"); search-library-content.md §4; view-card-detail.md Offline row | `tool/smoke/web.mjs` (offline leg) | PR #267 | **The shipping Web build cached nothing, so an offline start showed a blank page.** Every spec sheet promises offline works — *\"Offline dùng local index\"*, *\"render committed local data\"* — and the data half is true: the store is a Drift wasm database in the browser. The *shell* is not. It arrives over the network, and nothing was keeping it. **Flutter no longer registers a worker on a first visit.** `flutter.js` registers only when `serviceWorkerUrl` is passed explicitly — a path it warns is being removed (flutter/flutter#156910) — or when a registration already exists. `flutter build web` generates a bootstrap that passes only `serviceWorkerVersion`, so a fresh browser got no worker, ever. Verified by probe: `getRegistrations()` returned `[]` after a full load, while a manual `register()` on the same origin succeeded. **The first fix made it worse, which is the part worth recording.** Adding `web/offline_shell.js` satisfied Flutter's \"a registration already exists\" branch, so the *second* load registered Flutter's deprecated worker on top and it took the scope — two workers, one scope, neither caching the shell. `web/flutter_bootstrap.js` now omits `serviceWorkerSettings` entirely, which makes `loadServiceWorker` a no-op. The worker is **network-first with a cache fallback**, not the cache-first a precache manifest gives: online loads stay fresh, so a release can never be shadowed by a stale shell, and offline is served from what the last online visit fetched. Two things a first draft got wrong and the probe caught: the document was keyed under `/#/first-run`, so `/` missed on the next start; and a `type === 'basic'` filter silently dropped `main.dart.js` and CanvasKit, which arrive as `cors`. The smoke now cuts the network and reloads, and the screenshot shows Today rendering with no network at all. Proven by removal before it was written: without the shell, that same reload failed on `/ — net::ERR_INTERNET_DISCONNECTED`. It also asserts no second worker holds the scope, because a regenerated bootstrap would put the settings straight back. **Closes the Web half of `5.7.4`'s offline variant.** The Android half stays open, as it has no runner here |
| `int-94` | **Fixed** (2026-07-28) | FC-05, DG-03 | move-flashcard.md §1, §5, §6, §7 | `TEST-WBS-5.3.1C-move-duplicates`, `TEST-WBS-6.5-move-picker-states` | PR #266 | **A move could put two identical cards in one deck, which creating them could not.** §1 runs the duplicate check *\"trong target context\"* and §5's table ends with *\"Duplicate candidate → Duplicate resolution before commit\"*; the use case ran straight through. The pair-wide check the create flow uses is the wrong one here — a move never changes the pair, so it can only collide inside the deck it lands in. The move now returns `MoveDuplicatesFound` and writes nothing until an explicit keep-both retry, the same decision `create-flashcard.md` gives a new card. A hidden twin still counts: hiding is not deleting and the term is still taken (`int-93`). **Second defect:** the sheet closed on the action state, and a duplicate pause settles it exactly like a commit does — so the pause would have closed the sheet announcing *Card moved* over a card that never left. The commit is announced on its own tick, the shape `card-editor` already uses. **Third:** §6's *\"Nothing has changed\"* was missing — the generic error surface said only that something went wrong, leaving a learner to wonder whether the card half-moved while §7 promises the rollback is total. The typed rejections now get their own lines. **Fourth, a regression from `int-92`:** the no-destinations guidance keyed off an empty list, and the picker now lists every deck in the pair; a learner whose only deck was the card's own saw a lone disabled row and no explanation. It keys off *nothing eligible* instead. **Not built:** §5's cross-pair compatibility review, still a typed rejection; §4's per-row path and card count |
| `int-93` | **Fixed** (2026-07-28) | SR-01, DG-01 | hide-flashcard.md §1, §4, §8; search-library-content.md §1, §5 | `TEST-WBS-10.1-search-hidden`, `TEST-WBS-6.5-hidden-row-semantics` | PR #265 | **Hiding a card removed it from search, so the only way back was remembering its deck.** §1 of `hide-flashcard.md` is explicit — a hidden Card *\"vẫn tồn tại trong Leaf list và có thể tìm/filter/manage\"* — and §8 sanctions exactly one exclusion, fresh candidate queues. `searchLibraryContent` filtered `is_hidden = 0` beside `deleted_at IS NULL`, treating hidden as a soft delete. The search spec does not ask for that: §1 says results *follow* the visibility policy, and §5 lists a hidden result as a state the list must render. A repository test asserted the wrong behaviour, which is why it survived; it is rewritten rather than deleted, so the register can show what was pinned. The read-model now returns hidden cards flagged, and the row carries `Hidden` beside the term. **Second defect, same feature:** the deck-detail card row marked hidden with the crossed-eye glyph alone. `MxIcon` renders without a label, so §4's *\"semantic label/badge, không chỉ opacity/color\"* was met visually and not at all for a screen reader — the accessible name was the bare term. Both rows now lead with `<term>, hidden`. **Not built:** §2's Study-browse hide entry point, which waits on `int-5` |
| `int-92` | **Fixed** (2026-07-28) | DG-01, DG-03 | add-content-to-deck.md §1, §3 node F, §4 | `TEST-WBS-6.5-card-target-reasons` | PR #264 | **The card-target picker hid every Parent, the same defect `int-89` fixed one flow over.** §3's ineligible branch is *\"Disabled · choose child\"* and §4 draws the Parent row present but disabled beside the eligible ones. `cardMoveDestinationDecks` excluded any deck with children in SQL, so a learner looking for a Parent they knew existed found nothing there and no reason why — and §1's rule that a Parent takes no direct cards was enforced invisibly. The query now returns every deck in the pair with the reason it cannot take the card, and the row renders disabled through `MxTappable`'s null-callback path so it reads as blocked to assistive tech, with the reason in its accessible name. The card's own deck is marked *Already here* rather than dropped, which is the same honesty. A test pins the invariant that matters: the eligible subset must stay exactly the old target list, so the picker never offers a row the write would reject. **Not built:** §4's per-row card counts (`320 cards`), which need a count the picker does not read today |
| `int-91` | **Open — verified unreachable; guard attempted and withdrawn** (2026-07-27) | DG-03 | organise-deck.md §7, §8; `deriveDeckContentState`; WBS 4.3 triggers | — | (audit, no PR) | **The deck screen picks its branch from the child count alone, so a deck holding both direct cards and children would render as an ordinary Parent with its cards invisible.** §7 requires the opposite: block the actions, show a consistency error, offer only Refresh, and never show two lists. `deriveDeckContentState` has always thrown `DataCorruptionFailure` on this — the screen is the one path that never asks it, deriving the branch from `childDecks.isNotEmpty` instead. **Verified unreachable, not assumed:** the 4.3 triggers reject the mixed insert even from raw SQL inside a test, so no write path in or out of the app can create it. **A guard was written and then withdrawn.** It rendered §7's consistency error with a Refresh, but the only way to exercise it is to drop `trg_flashcards_insert_no_mixed_content` to simulate a corrupt store, and that test would not pass in the time available — it kept ending on a drift stream-teardown timer. Shipping a guard whose firing is unproven is the failure this register keeps recording, so the code went back rather than in. **What it would take:** a test that drops the trigger, inserts, and settles drift's teardown cleanly — or a `_DeckBranch` extracted far enough to be driven directly. Worth doing when the branch is next touched; not worth untested defensive code before then |
| `int-90` | **Fixed** (2026-07-27) | DG-03 | move-deck.md §1, §7, §8, §10 | `TEST-WBS-6.2-move-failure-copy` | PR #262 | **A failed move said only that something went wrong.** §8's copy is *\"Couldn't move the deck. Nothing has changed. Try again.\"* and the middle sentence is the one that matters: §1 promises the move is atomic — never in both places, never lost from both — and that line is where a learner is told so. The sheet showed the shared error surface instead, which leaves them wondering whether the deck half-moved and whether retrying would duplicate it. §7's structural rejections now get their own lines too: a destination sibling-name collision, a Leaf target, and a self/descendant target. Reaching one of those is not an ordinary error — the picker filters those destinations out, so it means the tree changed under the open sheet, which is §10's case — and naming the rule is more useful than flattening it into \"try again\". A missing destination maps to §7's stale-target line for the same reason. Audited alongside and **not** built: §6's cross-pair move, which the picker does not offer at all (it lists one pair), and §4's select-then-`Move here` composition, which this build collapses into a one-tap picker. Both are scope questions for `6.2` rather than defects in what ships, and recorded here so the next reader of this flow knows they were seen |
| `int-89` | **Fixed** (2026-07-27) | DG-01, DG-03 | move-deck.md §3 node E, §4, §5, §7, §11 | `TEST-WBS-6.2-move-reasons` | PR #261 | **The move picker hid every destination a learner could not choose.** §3's ineligible branch is *\"Disabled + helper\"*, §4 draws the blocked rows with their reason beside them (`⊘ <Leaf deck>  Contains cards`, `⊘ <Current deck>  This deck`), and §11 lists \"leaf/self/descendant disabled\" among the states to render. The SQL filtered them out instead, so a deck that holds cards and a deck that was never there looked identical — absent — and a learner hunting for a destination had no way to tell which. The query now returns every deck in the pair with the reason it cannot receive the move, and the row renders disabled with §7's copy shortened to §4's labels. Disabling is `MxTappable`'s own null-callback path, so a blocked destination reads as blocked to assistive tech rather than merely looking grey. A test pins the part that could drift: the eligible subset must stay exactly the old destination list, because §12 leaves the store the final say and a picker that offers more than the store accepts would trade a hidden row for a failed write |
| `int-88` | **Closed — an engine limit surfaced by the harness, not a product defect** (2026-07-27) | DG-03 | WBS 5.7.5 criterion 4; Flutter engine `EnginePlatformDispatcher.parseBrowserLanguages` | `tool/smoke/web.mjs`; `TEST-core-time-zone-fallback` | PR #253 | **The Tier-1 Web smoke failed in CI on a white screen with `RangeError: Incorrect locale information provided`, before the first frame.** Reproduced locally by giving the browser `locale: 'C'`, then traced with a profile build and the JS stack the release build had withheld: `new Locale` ← `EnginePlatformDispatcher_parseBrowserLanguages` ← `CanvasKitRenderer.initialize`. **The thrower is Flutter's engine, not MemoX.** A runner with no `LANG` makes Chromium report `C` in `navigator.languages`; `C` is a POSIX locale name, not a BCP-47 tag, and the engine constructs a JS `Intl.Locale` from every entry during renderer init. No app code has run at that point. So the fix is in the harness: it pins `en-US` instead of inheriting the runner's environment, because a browser reporting `C` is a property of a bare shell and not of any user — real browsers normalise `navigator.languages` to BCP-47. Unpinned, the smoke reported an engine limitation as a product failure. `MEMOX_SMOKE_LOCALE` still overrides it, which is how this was found and how it can be re-checked. **Two corrections to my own earlier claims, both already withdrawn in-branch:** the `SystemTimeZone.id` guard was announced as the fix and was not — it is unrelated hardening that stands on its own, since reading `DateTime.timeZoneName` unguarded is genuinely unsafe on Web. And criterion 4's Web half, reported red last round on this evidence, is green: the app boots on every locale a browser can actually report |
| `int-87` | **Fixed — and it had been red for twelve PRs** (2026-07-27) | DG-03 | §6.5 parity rule; WBS line 224 ("machine-enforced"); `.gitignore:71` | `tool/verify/parity_census.mjs` | PR #252 | **The parity census failed in CI on every pull request, and the local gate never saw it.** `docs/design/**/shots/*.png` is deliberately gitignored — 36 MB of reference images the build does not need — so a CI checkout holds twelve force-added shots and no more. The census only skips its dangling-reference check when it finds *no* shots at all; twelve is not none, so it reported seven correct rows as referencing shots that do not exist. Every one of the last twelve PR runs is red, including PRs #249 and #250, **which I merged without looking at CI**. The local gate passes because this machine has all 481 images. The check now compares what the checkout holds against what `shots/INDEX.md` names — the index is generated from the shot registry and is tracked, so it is the one thing a checkout without images can still read. Fewer shots than the index names means the checkout cannot judge references, and it says so instead of blaming rows; the coverage line stops printing arithmetic over two different sets (it read `76/6 shots have a row (-70 without one)`). Verified both ways: a simulated CI checkout now passes, and hiding one referenced shot in a full checkout still fails with exit 1. **A second defect the same run exposed:** the `web_smoke` job failed on its first real run because `*.g.dart` is gitignored, so a fresh checkout has no generated sources and the Web build dies on every `part` directive. `tool/verify/run.mjs` generates them for the jobs that call it; `web_smoke` and `visual_parity` build Flutter directly and had to ask for themselves. Both now do. `visual_parity` had the identical gap and had simply never run on a checkout that lacked them — leaving it broken would have shipped a merge gate that cannot execute. **A landmine defused in PR #254:** `visual_parity` is required by the CI gate whenever a PR changes a screen, and on a runner it would have spent minutes building a web bundle and driving a browser only to die inside the image diff with `ENOENT` on a PNG — a failure that reads as a broken spec rather than a checkout that never had the reference. `tool/parity/run.mjs` now preflights the shot set against the same tracked `INDEX.md` the census uses and stops immediately, naming the cause and this row. It exits non-zero rather than skipping: §6.5 says no screen-changing PR merges without parity evidence, and a checkout that cannot produce evidence has not met that. **Not fixed here, because it is the owner's:** with the shots ignored, CI's `visual_parity` job has nothing to compare against either, so §6.5's "machine-enforced" claim on WBS line 224 is not true of CI today. Making it true means tracking 36 MB of PNGs, moving them to Git LFS, or fetching the kit in CI — a repository decision, and the ignore was deliberate |
| `int-86` | **Fixed** (2026-07-27) | DG-03 | WBS 5.7.5 criterion 4 | `tool/smoke/web.mjs` | PR #248 | **Nothing had ever checked that the shipping entry boots.** Every Web check in the repo drives `lib/app/dev/parity_main.dart` — a debug entry that seeds fixtures, installs provider overrides and forces semantics on. `lib/main.dart` was built by no harness and loaded by no browser, so \"the app runs on Tier-1 Web\" was an assumption, not a result. `tool/smoke/web.mjs` now builds it for release, serves it, and drives it to the first-run landing with no console or page errors. **Extended in PR #250 past boot to the one thing a Web build can get wrong that no unit test can see: whether a write survives a page load.** It taps `Not now`, which `handle-empty-library-today.md` §4 makes a *persistent* skipped marker, lands on Today, then reloads — a fresh document, engine and Drift wasm worker — and fails if the router's first-run gate sends it back to the landing. That is the local-first promise checked on the platform where it is least obvious. `Not now` was chosen because it is the shortest journey that writes and needs no typing, which keeps this harness clear of the CanvasKit text-entry machinery the parity flows carry. Proven by removal: skip the tap and the reload lands back on the landing. **It also shipped with a defect of its own, found by the owner and fixed in PR #249:** the first version spawned `tool/parity/serve.mjs` as a child process. On Windows that child outlived every way the script tried to stop it — a shell wrapper swallowed `kill`, and a script killed mid-run never reached its cleanup at all — so the server kept holding its port and the run never terminated. One orphan sat for three hours with its work long finished, which is how it was noticed. The server is now created **in this process**: there is nothing left to orphan, because the listener dies with the run whatever kills it. **Two things the first runs taught, both kept in the harness:** the expectation was wrong before the app was — a fresh install lands on the first-run *landing*, not the language-pair setup, and only the failure screenshot showed that, which is why the failure path captures one (Flutter paints to canvas, so page text is empty and cannot say). And the shipping entry does not force semantics on, correctly; the smoke performs the same framework-owned accessibility activation the parity flows do, which makes it a screen-reader check as well — a shipping build whose semantics never appear would fail here |
| `int-85` | **Fixed** (2026-07-27) | DG-03 | answer-study-stage.md §3 node D, §5 "Empty/invalid answer không tạo Attempt", §6, §9 row 1 | `TEST-WBS-5.6.9-required-guidance` | PR #247 | **A blank keyboard submit in Fill did nothing and said nothing.** §6 asks the CTA to stay disabled until the required interaction is complete, and Fill does that — but the keyboard submits past a disabled button, and §3's invalid branch goes to inline guidance with §9 giving it copy (`Complete this step before continuing.`) that existed nowhere in the app. So a learner who pressed Enter on an empty field got silence from both the button and the field. The field now carries that guidance, set by the blank submit and cleared as soon as there is something to check. §5 is untouched — no attempt is created either way; what changed is that the refusal is now visible. **Audited and already right:** §9's save-failure and stale-checkpoint rows both have their copy (`int-40`), and the missing-prompt row is a recorded decision not to notify — the card is gone because the learner deleted it, and this build has no second writer to surprise them |
| `int-84` | **Fixed** (2026-07-27) | DG-03 | start-study-session.md §5 ("Different active session | Nêu Deck/scope và cho Resume/End before new start"), §4 | `TEST-WBS-5.6.1-active-session-deck` | PR #246 | **The already-running-session prompt would not say which session.** §5's row asks it to name the deck and scope before offering Resume, and the copy read \"A session is already in progress\" — so a learner who had left a session in one deck and tapped Study in another was offered a Resume that would take them somewhere they had not asked to go, with nothing on screen saying where. The start conflict carries only the fact that a session exists, so the running one is now read for its deck. A deck that cannot be resolved — deleted under a running session, which is `resume-study-session.md` §6's case — falls back to the unqualified copy rather than naming something that is gone. `Start over` is still absent for the reason `int-15` records: nothing owns what abandoning a session does to its committed goal and streak contributions |
| `int-83` | **Grading half fixed; board still ephemeral** (2026-07-27) | DG-03 | match-terms-and-meanings.md §4, §6; exit-study-session.md §5 last line; resume-study-session.md §1 | — | (audit, no PR) | **A Match board does not survive an exit, so a mid-round lapse is forgotten.** §4 puts remaining pairs, the next-round failed set and the tile order in the checkpoint, and §6 requires Resume to keep them; the board persists nothing and flushes per-card outcomes only when the round completes. Two consequences: the pairs already cleared are re-matched from scratch, and — the part that changes grading — a pair the learner got **wrong** before leaving comes back with no lapse recorded, so `nextRoundFailedCardIds` misses it and the card can complete the stage without ever appearing in a retry round. That is exactly what `exit-study-session.md` §5's last line forbids: \"paused checkpoint gồm ... next-round failed set đã commit\". **Update (PR #245): the half that changes grading is closed.** A lapse is now written into the checkpoint's failed set the moment it happens, so leaving no longer forgets it and the card still returns in a retry round. That needed no durable board and no migration: the failed set is already a checkpoint field, `saveCheckpoint` already exists for the pause path, and `SessionAdvancePolicy` **accumulates** onto the committed set rather than replacing it — so the round-end flush keeps what the lapse write put there. The write is best-effort by design: the board keeps its own copy for the flush, which is the path that reports failures, so a store that refuses it costs durability across an exit and nothing else. **Still open, and still WBS `5.6.12`:** the locked pairs themselves are not persisted, so the round restarts and cleared pairs are matched again. That is the cosmetic half, and `int-82` discloses it |
| `int-81` | **Covered** (2026-07-27) | DG-03 | fill-card-answer.md §4; guess-card-meaning.md; recall-and-self-grade.md; `int-78`, `int-80` | `TEST-WBS-5.6-retry-round-e2e`; `TEST-WBS-5.6.8-retry-round` | PR #243 | **The retry-round boundary is now driven for all three graded modes, not just Fill.** `int-78` was fixed in all three but proven in one; a fix nobody drives is a fix nobody knows about. Guess now crosses the boundary end to end over the real store — a wrong choice, the round closing, and the retry opening with nothing chosen *and accepting a new choice*, which is the half a carried-over selection made impossible. Recall crosses it at the screen level, asserting the retry gets a full 20-second budget rather than the clock the last round spent. Both were proven by pinning the round back to a constant, and both fail when it is. **Two harness facts worth keeping.** Recall owns a periodic timer, so `pumpAndSettle` refuses to return while its countdown lives — every step that can reach Recall has to advance frames explicitly, and a test that ends on it must unmount. And the end-to-end Guess case deliberately stops before committing its passing answer: passing stage 3 opens Recall, which drags that timer into a database-backed test where fake time and the real store do not settle together. That is why Recall's round coverage sits at the screen level instead |
| `int-80` | **Fixed** (2026-07-27) | DG-03 | fill-card-answer.md §4 ("round mới ... mở input trống cho attempt mới") | `TEST-WBS-5.6-retry-round-e2e` | PRs #242, #243 | **A Fill retry round re-opened with the failed answer still typed in it.** `int-78` moved the graded modes' provider state onto `(cardId, roundIndex)`, which fixed the grade, the hint and the countdown — but Fill's text controller does not live in a provider. It lives in this element's hooks, and the element is keyed by card, so it survived the round boundary along with its text. The new attempt therefore opened pre-filled with the answer that had just been marked wrong. Found by the new end-to-end layer on its first run, not by reading: every per-mode test mounts one fixed runtime and never crosses a round, which is exactly why `int-78` and this survived them. The stage is now keyed by round *and* card |
| `int-79` | **Fixed — and my reason for calling it blocked was wrong** (2026-07-27) | DG-03 | ST-SESSION-TYPE-v1 ST-TYPE-003/004/011; start-study-session.md §5 | `TEST-WBS-5.6.1-practice-scope` | PR #256 | **`SessionType.practice` could be planned and preferred but never started:** `StudyEligibilityPolicy` has a full Practice arm (ST-TYPE-004's mode check, empty scope, ST-TYPE-011's Guess pool), `SessionModePlanResolver` has one too, and `StartStudySessionUseCase._cardIdsFor` had none — so every attempt threw `unsupported-session-type`. One missing arm in one method, behind two layers that were complete. **I recorded this as owner-blocked on the grounds that \"which cards a Practice session draws is a product question the flow does not answer\". That was wrong, and I had not read the table I was citing.** ST-TYPE-003 answers it in its Given: *\"Practice scope hợp lệ\"* — the queue is the scope, not a schedule. Practice sets `scheduleSrs = false` and contributes no Goal or Streak, so a card's box does not decide whether it can be practised; the studiable set is the same hidden/deleted-excluded one the deck summaries count. A `studiableCardIdsInScope` query over the deck subtree now feeds that arm. The scheduled types are untouched and a test pins that they still read their own queues — Practice is an addition, not a change to them |
| `int-78` | **Fixed** (2026-07-27) | DG-03 | fill-card-answer.md §4 ("Hint state thuộc từng attempt và reset khi Card bắt đầu attempt ở round mới"; "round mới ... mở input trống cho attempt mới"), §6; guess-card-meaning.md; recall-and-self-grade.md | `TEST-WBS-5.6.9-retry-round` | PR #241 | **A one-card mastery retry round re-opened already answered, so the round could never be passed.** Fill, Guess and Recall each hold their pre-commit UI state in a provider keyed by `cardId` alone, with a comment saying that resets it when the round advances to the next card. It does — for the *next card*. A retry round re-opens the **same** card, so the widget element survives the boundary, the provider is never released, and the attempt begins carrying the previous round's state. With more than one card in the failed set the element changes on the way through and the bug hides. With exactly one, it is terminal: Fill re-opens graded — input locked, answer revealed, `Continue` showing — so the learner cannot answer, the round cannot be passed, and the session cannot finish. Recall re-opens on a countdown already at zero, timing the card out again the moment it opens. Guess re-opens with the previous round's choice selected. All three are now keyed by `(cardId, roundIndex)`, which is what §4 says the state belongs to. The hint half is also an evidence defect: a carried-over hint made the retry record `hintUsed` on an attempt the learner never asked for help with. **Checked and not a defect:** the attempt event ids omit the round (`fill-<cardId>`), which looked like it would collide across rounds — the idempotency key is built in `AnswerStudyStageUseCase` from `session:stage:round:position:eventId`, so the round is already in the identity |
| `int-77` | **Fixed** (2026-07-27) | DG-01, DG-03 | review-cards.md §1, §2, §4, §6 | `TEST-WBS-5.6.6-browse-gestures` | PR #240 | **The Review stage told the learner to swipe, and ignored every swipe.** §1 makes the gestures the flow's own navigation — left is Next, right is Previous — and §2's master flow names them on both edges. The stage rendered the kit's `Swipe to continue` hint between two chevrons over a surface with no gesture recognizer at all, so the single instruction on screen was the one thing that did not work. §4 also requires a keyboard equivalent \"không phụ thuộc gesture duy nhất\", and there was no key that moved a card either — on Tier-1 Web the stage could only be driven by clicking the chevrons. Both are now wired to the same two handlers the chevrons use, so all three routes agree: a right swipe on the first card changes nothing (§4), and browsing back to a card already seen records no second evidence (§4's \"không tạo reviewed evidence trùng\"). Additive only — no visual change, so the blocked `MX-VIS-036`/`037` composition is untouched. **Not built:** §3's labelled `Next`/`Finish` primary CTA, because the kit draws the swipe hint here instead; that is the same kit-vs-§4 contradiction `int-56` parks for these screens |
| `int-76` | **Fixed** (2026-07-27) | DG-01, DG-03 | relearn-cards.md §1 "User được biết còn bao nhiêu Cards cần relearn", §4; srs-binary-review.md §2; kit `study-session--relearn` / `--due-review` | `TEST-WBS-5.6-session-identity` | PR #239 | **A relearn session and a due review were the same picture with different consequences.** Both run the binary strategy on one screen under one title, and neither said which was running. §2 makes them genuinely different: a relearn grades from the card's *current* box and can promote it, a due review moves it along its schedule. And §1 of `relearn-cards.md` requires the learner to be told how many cards are left to revisit, which nothing did — a learner who tapped `Review missed` landed on a screen titled `Guess` or `Review` with a plain counter, nothing marking these as the cards they had just got wrong. The stage title is now the session's kind when it has one, and a toned note above the prompt says what answering will do; the note carries the remaining count for relearn, which is §1's requirement and had no other home. **One kit copy deliberately not adopted:** its Guess relearn variant reads \"Review this word — not counted toward progress.\" A relearn session in this build *is* counted — it schedules once per card and qualifies for the goal and streak — so the sentence would be a false statement about the product. Recorded here rather than followed |
| `int-74` | **Fixed** (2026-07-27) | DG-01, DG-03 | finalize-study-session.md §3 node B, §4 "không có CTA khi đang xử lý", §6 "Finalizing: stable progress/status", §10; kit `FinalizingView.jsx` | `TEST-WBS-5.6.13-finalizing`; `MX-VIS-081` | PR #238 | **The finalizing state was one centred sentence.** §6 asks it for a stable progress/status and the kit spends the whole screen on it — the hero, then the shapes of the three stats and the streak card that are about to arrive — so the layout holds while the commit runs. This rendered `Finalizing…` centred in an empty frame, which meant the screen jumped from a line of text to a full page the moment the commit landed, on the one screen where §6 asks for stability. Now the kit's composition, with no CTA per §4. The title adopts the kit's `Saving your results…`: no flow specifies this copy, and `Finalizing…` names the system's operation rather than the learner's. Measured at 1.00% light / 1.64% dark |
| `int-75` | **Fixed** (2026-07-27) | DG-01, DG-03 | finalize-study-session.md §6 "Retry: same finalize request identity", §9 state matrix (`retry`); kit `FinalizingView.jsx` `retry` | `TEST-WBS-5.6.13-retrying`; `MX-VIS-082` | PR #238 | **The retry state did not exist.** §9 lists `retry` beside `finalizing` as its own state and the kit reframes the whole view for it — `refresh` for `cloud_sync`, *Retrying…* for *Saving your results…*, and a body that says it is trying **again**. The app had one loading view, so a learner who had just watched a save fail pressed Retry and got the first-attempt copy back, with nothing acknowledging the failure they had just seen. The notifier now reports whether the finalize in flight is a re-attempt; the flag survives for the life of the screen because `finalize()` only ever runs from the not-yet-finalized state. Measured at 0.82% light / 1.44% dark |
| `int-72` | **Fixed** (2026-07-27) | DG-01, DG-03 | complete-daily-goal.md §1, §3 node F, §4, §8; kit `StudyResult.jsx` `goal-met` + `StreakGoalCard.jsx` | `TEST-WBS-5.7-goal-met`; `MX-VIS-080` | PR #237 | **The Study Result never told the learner they had completed their daily goal.** §3 routes the crossed-target transition to a goal-met result state and §4 gives its composition; the screen had one hero for every outcome and a progress bar that read `20/20` with nothing saying what that meant. Today's card already said `met`, so the two surfaces disagreed about the same fact — and the disagreement fell on the surface where the goal is actually completed. Now the kit's `goal-met`: the celebration glyph on the success tone, the title, §4's `<current> of <target> completed today`, and the kit's success badge on the streak card. The condition is §1's — reaching *or passing* the target, and never when no goal is configured, which is what `_goalStatus` already returns null for. Measured at 2.23% light / 2.18% dark (`MX-VIS-080`), which is §8's parity criterion for this half; the Dashboard half is the goal card that already exists |
| `int-73` | **Open — needs a product rule** (2026-07-27) | DG-01 | kit `StudyResult.jsx` `goal-missed`; complete-daily-goal.md §3 (met only) | — | — | **The kit has a `goal-missed` result state and no flow specifies it.** `study-result--goal-missed` swaps the hero for `trending_up` on the warning tone, titled *Almost there!* with *6 more minutes to hit today's goal*. `complete-daily-goal.md` owns the crossing and says nothing about falling short, and no other study-goal flow names a shortfall state. Two decisions are missing and both are the owner's: when a shortfall counts as *almost* rather than simply not met, and whether a finished session should be given a warning tone at all — a learner who studied and stopped is not in an error. Not built, because either answer would be an invented product semantic on a terminal screen |
| `int-69` | **Fixed** (2026-07-27) | DG-03 | manage-today-create-actions.md §4 "Double selection chỉ handoff một flow" | `TEST-WBS-5.7-create-single-handoff` | PR #236 | **A double tap on the create FAB opened two sheets.** The sheet takes a frame to cover the FAB, and a second selection inside that frame stacked a second sheet over the first — dismissing one left the other standing. The FAB now holds an in-flight flag for the whole handoff, which also covers the flow the sheet hands off to. Proven through the callback rather than two hit-tested taps: once the first sheet's barrier owns the FAB's position no second pointer reaches it, so a tap-driven test would pass with or without the guard |
| `int-70` | **Fixed** (2026-07-27) | DG-03 | handle-caught-up-today.md §4 "Browse return giữ Today context và refresh"; manage-today-create-actions.md §1, §6 "Success refresh Today đúng một lần"; refresh-today-projections.md §3 | `TEST-WBS-5.7-visible-refresh` | PR #236 | **Today did not refresh when it came back into view.** Library is a *sibling tab*, not a route over Today, so the shell keeps this branch mounted in its IndexedStack: `Explore decks` → browse → back to Today read the numbers from before the learner left, which is exactly what §4 forbids. The same hole sat under the create actions — `Add card` pushes the editor over the shell, so a learner who was caught up with nothing new to learn added a card and came back to a dashboard still offering them nothing. Both are now one trigger: `_TodayBody` re-reads whenever its subtree becomes visible again, read from `TickerMode`, which go_router mutes for an inactive branch and Flutter mutes for an obscured route. Owned once by Today rather than by every caller that leaves it — `int-67`'s hand-wired search call is removed as redundant, and the create handoffs need no call of their own. Two details the tests pinned down: a hook effect still runs inside the build phase, so the invalidate is deferred a frame; and the dialog-based create paths keep their explicit invalidate, because a dialog does not obscure what is under it |
| `int-66` | **Fixed** (2026-07-27) | DG-03 | refresh-today-projections.md §3 trigger contract; load-today-dashboard.md; start-review-from-today.md §4 | `TEST-WBS-5.7-foreground-refresh` | PR #235 | **Today never refreshed when the app came back to the foreground.** §3 names app foreground first among the triggers and nothing wired it, which matters more here than anywhere else: every number on this screen is derived from "now". A learner who left the app open overnight came back to yesterday's greeting, yesterday's due count, yesterday's streak, and a caught-up state that had stopped being true at midnight — with pull-to-refresh the only way to find out. `_TodayBody` now installs an `AppLifecycleListener` and re-reads on resume; the `setState` beside the invalidate is for the greeting, which reads the clock directly rather than through the projection. §1 needs no request token of its own — the provider element keeps only its latest future |
| `int-67` | **Fixed** (2026-07-27) | DG-03 | refresh-today-projections.md §3 "Deck/Card mutation"; open-search-result.md §4 | `TEST-WBS-5.7-search-return-refresh` | PR #235 | **Returning from search left Today on its pre-search snapshot.** Search is *pushed* over Today rather than replacing it, so Today stays mounted underneath and its projection survives the visit — and since `int-64` the search results open the card editor, where a card can be renamed or deleted out of the very counts the dashboard is showing. The app-bar action awaited the push and re-read on return. **Superseded by `int-70`** (2026-07-27), which replaced that hand-wired call with the branch-visibility trigger: the same refresh, owned once by Today rather than by every caller that leaves it |
| `int-68` | **Fixed** (2026-07-27) | DG-03 | refresh-today-projections.md §3 ("App foreground/day boundary"), §4 "Day boundary có thể đổi primary state và summaries cùng snapshot" | `TEST-WBS-5.7-day-boundary` | PR #255 | **A dashboard left open across midnight kept yesterday's day.** `int-66` wired the foreground trigger, which covers every way *back* into the app; this is the one way it does not. Nothing else fires there — no tab change, no route pop, no lifecycle event — and every number on the screen is derived from the day that just ended: the greeting still says good evening, the streak still counts yesterday, and a caught-up day quietly stops being true. Today now schedules one timer to the next local boundary, re-arming itself on each fire, read through the same clock and zone the rest of the screen uses so a test can move midnight. **Two things this round corrected in my own earlier reasoning.** I had recorded it as needing "a scheduled tick this screen has no other reason to own" and deferred it twice on the belief that a pending timer fails a widget test at teardown. It does not — a one-shot timer held by a mounted widget is harmless, proven before designing around it, and all 48 existing Today tests pass unchanged. And the first implementation built the boundary with `DateTime(now.year, now.month, now.day)`, which is *host-local*, while `localTimeOf` may return a UTC-kind reading of another zone; subtracting the two measured the gap between two zones' midnights and fired immediately on a UTC+9 host. The delay is now time-of-day subtracted from a day, which is the same answer in any kind. The test caught it |
| `int-62` | **Fixed** (2026-07-27) | DG-01, DG-03 | search-library-content.md §6; filter-search-results.md §4; kit `Search.jsx` `search/no-results`; KIT-26-02 | `TEST-WBS-10.2-no-results-state` | PR #234 | **No-results and "you haven't searched yet" were the same picture.** Both rendered one centred caption in the same role, so a learner could not tell a query that found nothing from a screen that had not been asked anything. The kit's `search/no-results` is a warning-toned empty state — `search_off` tile, title "No matches", and copy naming the query back — which is also what KIT-26-02 requires: no-results must read differently from an empty dataset. The body now echoes the query, so the learner can see what was actually searched for before deciding whether it was a typo |
| `int-63` | **Fixed** (2026-07-27) | DG-01, DG-03 | filter-search-results.md §4 "No-results-with-filters nêu Clear/adjust filters", §6 "Clear phục hồi query không filter"; KIT-26-03 | `TEST-WBS-10.2-filtered-empty` | PR #234 | **A filter that hid every hit claimed the query matched nothing.** Selecting `Decks` on a query that only matched cards produced the no-results copy — a false statement, because the results existed and one chip away. §4 requires that state to name Clear/adjust filters and §6 requires a clear that restores the unfiltered query; the only way back was guessing that `All` was a reset, which nothing said. The filtered case is now its own empty state (`filter_alt_off`, "No results of this type") carrying a `Clear filter` action, and it is chosen on `hits.isNotEmpty` so it can never stand in for a genuine no-match |
| `int-64` | **Fixed** (2026-07-27) | DG-03 | open-search-result.md §1, §2, §4 "Sau edit/delete, return refresh affected result/index" | `TEST-WBS-10.2-return-refresh` | PR #234 | **Returning from a result never refreshed what listed it.** Search stays mounted under the pushed route and its list is a future cached per query, so a card renamed in the editor came back to a row still carrying its old term, and a card deleted there came back to a row that opened an object no longer in the store — §2's "Deleted/hidden object không mở route stale", reached by the app's own edit flow rather than by any race. The navigation helpers now return the push future and the row invalidates its query's results when that future completes, so the return re-reads the store. The seam is exactly the one the register keeps finding: the writer (the editor) and the reader (the result list) were each correct alone |
| `int-65` | **Fixed** (2026-07-27) | DG-03 | open-search-result.md §4 "Double tap chỉ tạo một navigation" | `TEST-WBS-10.2-single-navigation` | PR #234 | **A double tap on a result pushed the destination twice.** The pushed route takes a frame to cover the row, and a second tap inside that frame stacked a second copy that Back then had to be pressed twice to leave. The row now holds an in-flight flag for the life of the push and ignores taps while it is set |
| `int-59` | **Fixed** (2026-07-27) | DG-01, DG-03 | manage-recent-searches.md §3, §4; kit `Search.jsx` `search/recent-remove-*` | `TEST-WBS-10.2-recent-remove` | PR #233 | **A recent search could only be dropped by wiping the whole history.** §3: "Row có query và remove action", and the kit draws it as the row's trailing `close` beside the `Clear all` link. The app had the link and no row action, so removing one query cost the learner the other seven. Removal matches on the same normalized key `record` dedupes by — removing works on what the learner sees rather than on exact bytes — and §4's idempotence is explicit: removing a query that is not there rewrites nothing rather than failing. The command now assigns its outcome to state instead of discarding it. §4 says "Failure không giả xóa": on failure the list is left alone *and* the failure is now readable, where before the clear path swallowed it and the row simply stayed |
| `int-58` | **Fixed** (2026-07-27) | DG-03 | search-decks.md §5; edit-deck.md §1; migration-policy.md rules 1, 3, 6 | `TEST-WBS-10.2-description-match`; `TEST-WBS-4.7-v3` | PR #232 | **The deck description had a writer and no reader.** `int-57` gave it an editing path the day before; nothing anywhere consumed it. `search-decks.md` §5 is the one place that says what it is *for* — "Match trên Deck name; description là supporting match", with "Exact/prefix name đứng trước partial description match" — and the query matched names only, so a deck whose description named the query was unfindable. Matching it needed the same NFC + case-folded key the name has a column for, because SQLite cannot compute one: `LOWER` is ASCII-only and would have worked for English and failed for every language this app is built to teach. So schema v3 adds `normalized_description`, and the search's third tier sits behind every name hit. A deck matching on both is carried by its name row rather than listed twice. **The migration backfills, and the note explains why that is allowed here.** v2's own note refuses to backfill `srs_activated_at` because the value was never recorded and inventing it would be a fabrication; this value is a pure function of data the row already holds, so deriving it is not the business-policy inference rule 6 forbids — and without it, every deck written before v3 would stay unsearchable until someone happened to edit it. **Correction to `int-43`:** it recorded that "the project has no migration files yet". It has the full apparatus — a policy doc, versioned snapshots under `drift_schemas/`, generated verification helpers, and a guided step-by-step upgrade. That claim is struck |
| `int-57` | **Fixed** (2026-07-27) | DG-03 | edit-deck.md §1, §4; create-deck.md §12 "Rename/edit description"; kit `deck-settings--rename` | `TEST-WBS-6.1-edit-description` | PR #231 | **A deck's description could be written once and never changed.** The column exists, `Deck.description` exists, the first-run setup form collects it, `create-deck.md` §12 lists "Rename/edit description" among the actions a created deck allows, and `edit-deck.md` §1 calls it optional metadata — and the only editing path in the app was a name-only rename dialog. So a description entered during first-run was permanent, and one never entered could never be added. Both fields now save in a single write, which §1 requires outright: "Save failure giữ toàn bộ draft; không autosave một phần form." A cleared field persists as absence rather than an empty string, so clearing reads the same as never filling it in. **One disagreement recorded rather than settled:** §4 draws this as a full-screen Settings form with the language pair shown read-only; the kit draws `deck-settings--rename`, a name-only dialog. The dialog is the kit's and the fields are the spec's. Choosing the archetype is an owner call of the same kind as `MX-VIS-036`'s, and no measured row constrains it — that shot has no `MX-VIS` row |
| `int-56` | **Recorded — owner ruling already covers it** (2026-07-27) | DG-01 | kit `ReviewMode.jsx` vs the `study-session--*` shots; owner rulings 2026-07-25 / 2026-07-26 | `MX-VIS-078`, `MX-VIS-079` | PR #230 | **The study states' residual is not `int-5`, and not the fixtures either — it is the kit disagreeing with itself, in the class the owner has already ruled on.** `ReviewMode.jsx` gives a study screen a nested app bar (back arrow, mode title, `format_size`, `more_vert`) with the progress header as the body's first child, which is exactly what this build renders. The `study-session--*` shots draw another shell: an X, the progress inline in the bar, a single overflow. Same shape as `MX-VIS-036`/`037`, one screen over, and the standing rulings settle it — the JSX is the standard and the shots will not be regenerated. **Measured rather than assumed.** The obvious suspect was the fixture: the shot stages a 25-card session on its 21st card prompting `school` while the fixture was five cards on the first. Seeding the shot's own scenario moved `MX-VIS-078` 24.50 → 24.88 — four tenths of a point, in the wrong direction. So the counter and the prompt were never the residual, and the shell is. The fixture change stays because it is the right fixture for the row, not because it helped. What remains buildable on that bar is the two actions the JSX names, and both are product-undefined, which is `int-5`'s open half |
| `int-55` | **Open — owner ruling on copy** (2026-07-27) | DG-01 | exit-study-session.md §4 vs kit `study-session--exit`; §6.5 | `MX-VIS-079` (measured) | PR #229 | **The exit confirm clipped its own actions to `Keep s…` / `Save a…`.** Found by measuring the state `int-54` had just re-composed: the kit's action row is two block halves, which fits its own labels (`Stay`, `Leave`) and not this app's (§4's `Keep studying`, `Save and exit`). The actions now keep their natural width and wrap, so nothing is clipped in any locale — and the dialog grows a row taller than the shot, which is most of the 28.59% / 28.96% now recorded. The trade is measured rather than argued: block halves put `MX-VIS-078` at 18.82 / 18.72 and clipped this state; natural widths put it at 24.50 / 24.92 and clip nothing. The choice is a copy question, not a layout one — §4's words say that leaving *saves*, which `Leave` does not, so shortening them costs meaning. Recorded for the owner: adopt the kit's labels and the kit's row, or keep the spec's and accept the taller dialog |
| `int-54` | **Fixed** (2026-07-27) | DG-01 | kit `_shared/ConfirmDialog.jsx` + `Dialog` helper vs `MxDialog`; §6.5 | `MX-VIS-078` (measured) | PR #227 | **The app has one dialog composite where the kit has two, and the confirm path got the wrong one.** The kit's icon `Dialog` is a centred column — icon tile on top, title and text under it, an actions row at full width. `MxDialog` is the kit's *`FormDialog`* shape: left-aligned title, body, right-aligned wrapped actions. That is correct for create/rename, which is what it was built for, and wrong for every confirm: they render title-above-icon, left-aligned, with stacked natural-width buttons. Found by measuring, not by looking — `MX-VIS-078` came in at 21.49% / 21.42% with the state itself correct, and the capture is what showed the layout underneath it. Six sites share the composite (exit-session, delete deck, delete card, discard draft, and this), so the fix is a shared-widget change with its own parity checklist, and it is worth its own round rather than a corner of this one. `MxDialog` now carries both of the kit's shapes: the form layout it always had, and the icon layout it never did — tone tile, centred copy, actions sharing one full-width row. Every confirm in the app takes the second one, which is five sites besides this state. `MX-VIS-078` moved 21.49 -> 18.82 light and 21.42 -> 18.72 dark on the change; what remains is the shot's own scenario and the app-bar divergence `int-5` owns, neither of which is the dialog. **Danger is no longer derived from tone.** An error-toned dialog used to force a red confirm, so this state's `Retry` was styled as destructive; the kit tones the tile and leaves the confirm primary unless the action itself destroys something. Every destructive call site already passed `danger: true` explicitly, so nothing lost its fill. **And the change caught a parity regression that was not mine to expect:** `MX-VIS-059` went 0.97% -> 5.83% because journeys that create their own preconditions now raise a success snackbar (`int-36`) that is still on screen at capture time. `expectStableCapture`'s three-identical-captures rule cannot see it — a stationary bar is perfectly stable — so the harness now waits for a confirmation to leave before capturing |
| `int-53` | **Fixed** (2026-07-27) | DG-01, DG-03 | kit `StudyResult.jsx` finalize-error branch; §6.5 parity rule | `MX-VIS-077` | PR #226 | The finalize-error state `int-49` built had no `MX-VIS` row, so nothing had ever compared it to the kit. Measuring it found a composition close but not equal: the kit tones the tile **error** (this had warning), labels the actions `Retry` + `Not now` (this had `Try again` + `Back to Today`) and gives the primary the refresh glyph. The title and body already came from the kit, so the state was speaking in two voices. **Now measured: 1.61% light / 2.50% dark**, and two of the corrections were the measurement's rather than the eye's. The action column is `--memox-size-3xl` — the `standard` width; `wide` overshot and painted past the kit's buttons on both sides, which is what the first 3.47% dark run was mostly made of. And the bottom-nav reservation stays: removing it, on the theory that a route with no nav should not reserve for one, took the diff *up* to 7.74% / 9.36%. The kit's `.app__body` reserves it whatever the route carries. Two more states of this screen have kit shots and no rows — `study-result--finalizing` and `--retry-finalize` — and `study-session--answer-save-error` covers the dialog `int-40` built. All three are reachable the same way and are worth a round each |
| `int-52` | **Fixed** (2026-07-27) | DG-01, DG-03 | organise-deck.md §1, §2; add-content-to-deck.md §1; kit `flashcard-list` vs `subdeck-list` | `TEST-WBS-6.1-leaf-fab` | PR #224 | **A Leaf deck promoted the one action its own contract blocks.** The kit draws two different FABs — `flashcard-list`'s adds a card, `subdeck-list`'s creates a deck — and this screen rendered the create-deck one on both branches. `organise-deck.md` §2 is unambiguous: "Leaf | Attempt tạo/move child vào | Bị chặn; yêu cầu đưa toàn bộ card ra trước". Nothing stopped the learner reaching for it; the store's exclusivity trigger did the refusing, after they had opened a dialog and typed a name. Meanwhile `add-content-to-deck.md` §1's "Local Add Card từ Leaf/Empty dùng current Deck" had no FAB at all — only a button at the foot of the card list. The FAB now follows the branch, which is what the kit does: Parent creates a deck, Leaf adds a card, Empty shows neither because it has not yet committed to either and offers both inline. **Follow-up, closed the same day:** the Leaf list's trailing `Add card` button was a second copy of the FAB's new action, and the kit draws no such button. It is gone. The parity question it was held for turned out to be settled already: `MX-VIS-042` is the row for this state, and it is blocked on filter chips (10.2), SRS counters/badges (5.4.2) and the breadcrumb (6.2) — unbuilt composition a capture would only photograph the absence of. The row's own note had recorded this exact swap as part of closing it, which is where the `AddCardSheet` question is answered too: the kit's sheet offers `Import cards`, nothing imports anything, and a row that opens nothing is the dishonesty `MX-VIS-076` already refuses |
| `int-51` | **Fixed** (2026-07-27) | DG-03 | answer-study-stage.md §6; WBS 5.6.7 | `TEST-WBS-5.6.7-selection-survives` | PR #222 | **A failed save took the learner's Guess choice off the screen while the dialog told them it was still there.** The screen cleared the selection the moment Continue was tapped — synchronously, before the save was known to have landed. `Fill` next door had the answer already: its feedback is keyed by `cardId`, so the next card starts clean without anyone clearing anything. `GuessSelection` is now keyed the same way and the manual clear is gone, which makes the promise in §6's copy true on screen as well as in the command. Auditing the other four mode commands turned up nothing: Fill's grading and Recall's timer are pure presentation and write nothing, and every mode but Match commits through the one answer command `int-40` already covers. Match was the exception (`int-50`). The test drives the dispatcher rather than the bare screen, for a reason worth keeping: mounted alone, the screen has nothing holding the answer command alive, and it is disposed mid-save. In the app the dispatcher's own listener is what keeps it — a bare-screen harness would have been testing a shape the app never runs |
| `int-50` | **Fixed** (2026-07-27) | DG-03 | answer-study-stage.md §6; WBS 5.6.6 | `TEST-WBS-5.6.6-flush-failure`; `TEST-WBS-5.6.6-flush-retry` | PR #221 | **Match had no failure surface at all.** `int-40` mounted the answer-save dialog on the dispatcher, but Match does not answer a card — it commits a whole board through its own command, which that listener never sees. Tapping `Next round` on a save that could not land did nothing, and the finished board sat there looking finished. Wiring the retry turned up a hazard in the retry itself. The flush writes card by card, so a failure halfway leaves a committed prefix; the retry re-sends the whole board, and the idempotency key would not stop the duplicate because by then the cursor has moved and the key is built from the position. Two things now prevent it: an input whose card the cursor has passed is skipped, and the flush reads the runtime it starts from rather than trusting the cached one — which was the same value from *before* those writes, so the skip would have compared against a stalled cursor and let the prefix through anyway. The invalidate is now unconditional for the same reason: a failure halfway still moved the store. The old flush test passed a fake that returned the same runtime for every call, so its cursor never moved. That was invisible while nothing depended on the movement; it is why the fake now advances, as the real use case does |
| `int-49` | **Fixed** (2026-07-27) | DG-01, DG-03 | finalize-study-session.md §6; WBS 5.6.13 | `TEST-WBS-5.6.13-finalize-error` | PR #220 | **The finalize error was the worst dead end of the three, because this screen has no back by design** — the Result is terminal and exits only through its own actions. It rendered the shared compact banner: a title, a small Retry, nothing else. A finalize that kept failing left the learner holding one button with nowhere to go. The body that explains what happened had been written and never rendered — the verifier's retained-strings list even carried it with the note "the screen currently shows the title alone", so the gap was known and parked. It is now on screen, and the retained entry is gone with it. The second exit is safe rather than merely convenient, which is the part worth checking: the session stays active, Today offers it back, and opening it finalizes again under §6's "same finalize request identity", so the deferred attempt counts once and not twice. Nothing is lost by walking away; only the schedule update waits. Third of three in this audit (`int-47`, `int-48`), and the pattern behind all of them is one shared error surface — a compact banner built for a screen with chrome around it — reused on full-screen routes that have none |
| `int-48` | **Fixed** (2026-07-27) | DG-01, DG-03 | resume-study-session.md §4, §8; WBS 5.6.12 | `TEST-WBS-5.6.12-resume-error` | PR #219 | **A resume that kept failing had the learner cornered.** The study route rendered the shared compact banner — a title and a small ghost Retry — on a full-screen page with no tab bar and no back. §8 names the missing exit outright: "Back từ resume error về Dashboard, giữ session paused", and §4 gives the state a composition of its own with the line that matters most to someone whose session will not open: the saved answers are still there. Neither was built. The state now owns the frame, carries §4's reassurance, and offers both ways out. `Start fresh` is still absent and the reason is now written where the code is: ending a running session is the undefined act `int-15` records, and here it would discard answers whose count the learner can see. Leaving for the dashboard is not that act — §8 is explicit that Back keeps the session paused — which is what makes the second exit safe to offer. The same shape as `int-47` one state over: both were full-screen dead ends behind a spec line that named the way out |
| `int-47` | **Fixed** (2026-07-27) | DG-03 | resume-study-session.md §3 node E, §7; WBS 5.7.3 handoff row | `TEST-WBS-5.7.3-resume`; `TEST-WBS-5.6-no-session-recovery` | PR #218 | Two halves of the same handoff. The **resume** direction is now driven end to end: a session left half-answered takes Today's primary action over a new review, and Resume lands on the card that was never answered rather than starting the queue again — asserted against the runtime, so "the right card" means the committed checkpoint's card and not merely a card. The **unavailable** direction was a dead end. §3 node E and §7 ask for recovery copy and a route back to the Dashboard; the study route showed a bare title on a full-screen page with no tab bar and no back. A learner who opened it after their session was finalized elsewhere had no way out of it at all — not a rare state, since the route is exactly where a stale link or a second surface lands them. It now says what probably happened, says the saved answers are untouched, and offers the way back §7 names |
| `int-46` | **Fixed** (2026-07-27) | DG-03 | WBS 5.7.3 handoff row; load-today-dashboard.md §6; refresh-today-projections.md §3 | `TEST-WBS-5.7.3-round-trip` | PR #217 | The join `int-45` did not cover: Today's own read deciding what to offer, the route it hands off to, and the same read afterwards deciding it is done. Every defect this session found on this path — the due count that counted decks (`int-26`), the projection that discarded a streak (`int-11`), the session that could not finish (`int-44`) — was invisible from one side of a boundary, and this drives all of it from Today's CTA to the Result's exit and back. Two facts about testing this stack came out of writing it, and are recorded in the test rather than in anyone's memory. `pumpAndSettle` hangs on Today: the loading state shimmers and a repeating animation never settles. And a command that reaches a real database does not progress inside the test's fake-async zone at all — the start sat in `AsyncLoading` through any number of pumps until `runAsync` gave its I/O real time. Both are the kind of thing that reads as a product bug when it is really the harness |
| `int-45` | **Fixed** (2026-07-27) | DG-03 | WBS 5.6.14 pyramid row ("widget/golden and interruption/restart E2E"); answer-study-stage.md §6 | `TEST-WBS-5.6.14-finish-e2e` | PR #216 | **Nothing drove the core loop end to end through the widgets.** Every layer under it had tests and every layer was right, which is exactly how `int-44` survived: the dispatcher, the mode screen, the answer command, the store, the re-read every answer performs and finalize were each correct alone, and the session still could not finish. The domain tests pass the runtime returned by `answer()` from step to step, so they never take the path the app takes — the one that goes back to the store. The gap is now covered by a test that starts a real due-review session on a real database with the real providers and answers it to the end, asserting the Result screen opens with the summary and the session row reads `completed`; a second case answers one card wrong and asserts the retry round opens instead. Removing the `int-44` fix turns the first one red, which is the whole point of writing it. This is the row `5.6.14` names and the layer `5.7.5`'s second criterion leans on |
| `int-44` | **Fixed** (2026-07-27) | DG-03 | resume-study-session.md §5 "Completed session luôn route Result"; finalize-study-session.md | `TEST-WBS-5.6.12-finished-reads-finished` | PR #215 | **A finished session came back as one sitting on its last card.** The complete phase lives in memory on the position the advance policy returns; the checkpoint has no column for it, and the finish branch persisted a cursor pointing *at* the final card rather than past it. The next read — a restart, or the ordinary invalidate every answer performs — rebuilt the session as `inRound` with that card current. The learner is asked the card they just answered, the result screen never opens, and finalize never runs, while §5 says a completed session always routes to Result. Found while building `int-43`: skipping the last card of a vanished round left the session on that card forever, which is the same defect reached by another road. The fix makes the persisted cursor honest — a finished round leaves it one past the end — and reads completion back from it, since that is the one thing the store can carry. Nothing else depended on the old value: the progress counter reads better as `3/3` than `2/3`, and finalize aggregates attempts, not positions. Proven by removing the derived phase and watching a session answered to the end come back unfinished |
| `int-43` | **Fixed** (2026-07-27) | DG-03 | ST-CONTENT-CHANGE-v1 ST-CHG-005, ST-CHG-006, ST-CHG-010; resume-study-session.md §6 | `TEST-WBS-5.6-skip-unavailable` | PR #215 | **A card deleted or hidden after the snapshot was still asked, graded and scheduled.** ST-CHG-005/006 require it to be skipped and never substituted; ST-CHG-010 requires the skip to leave the accuracy denominator and to schedule nothing. The runtime rendered straight from the frozen snapshot, so a session quizzed the learner on a card they had just deleted and then wrote SRS state for it. The skip commits like any other advance and writes no attempt, which is what keeps it out of the denominator and out of the scheduler — both follow from attempts, so nothing had to be special-cased to exclude it. It advances as *passed* in the narrow sense of "does not seed the retry round": a skipped card must not return for an attempt it can no longer have, and ST-CHG-005 is explicit that a skip fakes no outcome either way. The audit reason needs no schema slot after all, which is what the earlier recording got wrong: a delete is a tombstone and a hide is a flag, so the row that explains the skip is still in the store and still says why. The reason is returned rather than stored |
| `int-41` | **Fixed** (2026-07-27) | DG-03 | exit-study-session.md §3, §5, §9; WBS 5.6.12 scope line | `TEST-WBS-5.6.12-recall-timer` | PR #213 | **The Recall countdown did not survive leaving the session.** §5: "Recall countdown trước reveal | Pause và persist `remainingMs`; Resume tiếp tục, không reset", and §9 makes it an acceptance criterion; WBS 5.6.12 names it in the row itself. The schema had carried the slot for it from the start — `study_checkpoints.timer_state_json`, deliberately opaque and versioned — and nothing had ever written to it. Leaving a card eight seconds from its deadline and coming back handed the learner a fresh twenty. `RecallTimerState`'s own doc comment recorded the debt ("the durable cross-exit timer lands with Exit/Resume") without anything tracking it. The countdown is now a versioned payload carried on the checkpoint and decoded onto the runtime, so the read path a resume already uses brings it back. It is card-scoped on purpose: a payload for a card the session has left is not applied to whatever is on screen now, and any answer clears it, since the answer path writes `{}` over the slot as it advances. Exit writes it only while the clock is still running — a revealed or timed-out card has nothing left to resume. Pause and resume are driven over one store, because the write is only worth anything if the read that follows finds what it left, for the card it left it on; the screen-level seed is proven separately by letting a restored eight seconds run out at eight rather than at twenty |
| `int-40` | **Fixed** (2026-07-27) | DG-03 | answer-study-stage.md §6, §9 | `TEST-WBS-5.6.3-answer-failure` | PR #212 | **A failed answer save was invisible in every study mode.** §6: "Failure dialog: `Couldn't save your answer. Your answer is still here.` + `Try again`." `StudyAnswerViewmodel` mapped the failure to a typed `AsyncError` exactly as designed — and its own doc comment says "the caller keeps the answer for Retry" — but no caller read it. All five mode screens only `read(...notifier)`. A save that failed therefore looked identical to one still in flight: the card stayed, the answer stayed, nothing was said, and the learner's only clue was that tapping again did nothing either. This is the core loop of WBS 5.6. The listener is mounted on the dispatcher rather than in five screens: the modes all submit through one command, and the dispatcher is the widget that outlives whichever stage is on screen. The command now holds the failed input so `Try again` re-submits *the same* attempt — §6 requires a retry not to write a second record, which the attempt's own request id already guarantees, but only if the retry carries it. §9 gives a stale write its own copy and its own action, so a `ConflictFailure` asks for a reload of the committed session instead of pushing the same answer at a checkpoint that has moved. Three tests: the dialog, the retry carrying the same answer, and the stale branch |
| `int-39` | **Fixed** (2026-07-27) | DG-03 | relearn-cards.md §1, §6; start-study-session.md §3 node C | `TEST-WBS-5.6.13-relearn-start` | PR #211 | **`Review mistakes` swallowed the only failure it could have.** The control returns a bool and the screen navigated on true; on false it did nothing at all — no message, no state change, the same do-nothing tap the control had before it started a session in the first place. §6 names the copy for exactly this: "Couldn't save the relearn queue. Your answers are safe." It now returns the start's outcome instead of a bool — null when there was nothing to start, so a benign tap stays quiet — and a failure reports in an error bar with the link still there as the retry. The audit also found the tap unguarded against itself. A second tap while the first start is in flight reaches `StartStudySessionUseCase`, which finds the session the first is still creating and throws `ConflictFailure(code: 'active-session')` — one request reported as a conflict, and now that failures are visible it would have shown the queue message for something that is not a queue problem. An in-flight flag drops the repeat. Both are proven by removing them and watching the tests fail |
| `int-38` | **Fixed** (2026-07-27) | DG-03 | set-appearance-preference.md §2, §4, §6 | `TEST-WBS-8.1-appearance-failure`; `TEST-WBS-8.1-appearance-latest` | PR #210 | **The appearance write was fire-and-forget.** `select` called the command without awaiting it and popped immediately, so a preference that never persisted closed the sheet exactly like one that did, and the "Restore prior · Retry" §2 asks for had nowhere to happen. The sheet now closes on success and, on failure, stays open with the prior selection still checked — the rows themselves are the retry — under the reason why. Auditing the rest of the flow found the second half of §4 unimplemented: "Rapid selection chỉ latest value thắng." Two taps started two commands with nothing deciding which one still spoke for the learner, so a superseded tap's late result set the state and invalidated the read. The writes were never the problem — drift runs statements on one connection in submission order, so the stored value is the last one tapped — but the *results* return whenever they return. A per-selection counter now discards anything a later tap has replaced. Proven by removing the guard and watching the test fail: a superseded failure became the command's state, and the sheet would have reported a failure for a choice the learner had already changed |
| `int-36` | **Fixed** (2026-07-27) | DG-01, DG-03 | create-flashcard.md §7; edit-flashcard.md §7; hide-flashcard.md §7; delete-flashcard.md §7; move-flashcard.md §7 | the five flashcard flow tests | PR #208 | The flashcard half of `int-33`: `Card added`, `Card updated`, `Card moved`, `Card hidden` / `Card visible`, `Card deleted` — all specified, none built. The create-another branch is the one that needed it most: nothing navigates, so a cleared form was the only sign the card had been written at all. Writing the editor tests turned up a harness fact worth recording: the other editor harnesses mount the screen at `home`, so a save that pops leaves the navigator empty and the messenger with no Scaffold to draw into. The confirmation is fine — the *harness* has nowhere to put it. The edit case is now driven through a host route the editor is pushed over, which is how the app opens it, and asserts the bar lands on the screen the editor returns to |
| `int-34` | **Open — specified after all; blocked on a schema change, not on the owner** (2026-07-27) | DG-03 | ST-CONTENT-CHANGE-v1 ST-CHG-002; resume-study-session.md §6; delete-deck.md | — | (audit, no PR) | **Deleting a deck during a session deletes the session out from under it.** The delete chain added in `int-25` includes `deleteSessionsInDecks`, so a running session whose deck is in scope has its row, attempts and snapshots removed while the study screen is still on it; the next read finds no session. **Correction to this row (2026-07-27): I recorded this as unspecified — \"`delete-deck.md` says nothing about sessions at all, so there is no rule to implement\" — and cited the reset spec's neighbouring clause as the only precedent. That was wrong. It is specified twice.** ST-CHG-002: *\"Deck deleted, snapshot content đủ | Continue | Cho hoàn tất snapshot; return Library\"*, and `resume-study-session.md` §6 carries the same row. So the behaviour is not a product call: the session must be allowed to finish from its snapshot, and Done returns to Library. What ships today destroys committed attempts instead, which is a spec violation with data loss. **Why it is still open, and this part is a real constraint rather than an unread document:** `study_sessions.deck_id` is `NOT NULL REFERENCES decks (id)`, so a session cannot outlive its deck without a schema change — the snapshot itself is already self-sufficient (`study_session_cards` carries term and meaning). Blocking the delete instead is *not* an available shortcut: the table's acceptance line forbids exactly that — \"không có flow riêng được tự chọn `skip` hay `block` khác bảng\". **Disclosure landed in PR #258, the same split `int-82` used for Match.** §1 requires an impact summary and §5 enumerates what goes with the deck; a running session's committed answers went too and were the one loss the summary never named. The confirm now says so, and only when it is true: the condition is the session's own deck sitting inside the deleted subtree, because a subtree session rooted *above* keeps its row and can still finish from its snapshot — which is ST-CHG-002 and ST-CHG-006 read together. **Correction to my own sizing (2026-07-27).** I wrote that this needed \"a schema v4 making `deck_id` nullable\" and called it the same shape as `int-58`'s v3. That underestimated it, and the underestimate came from looking at one foreign key. Letting a session outlive its deck means dropping **three**: `study_sessions.deck_id → decks`, `study_session_cards.card_id → flashcards`, and `session_relearn_items.card_id → flashcards` — the delete chain removes the cards too, and the snapshot rows that make \"finish from the snapshot\" possible are anchored to them. `study_attempts` is already detached from cards by `clearTerminalAttemptRefsInDecks`, which is the one place this pattern exists today. **So this is an owner decision after all, but a well-posed one rather than the vague \"product call\" I first recorded.** ST-CHG-002 requires a session to finish after its deck is deleted; the schema deliberately anchors sessions, snapshots and relearn items to decks and cards by foreign key. Honouring the table means dropping those three and accepting rows that reference deleted content — a real weakening of referential integrity well beyond this case. Amending the table is the other side. Choosing between them is an architecture call, not one to infer, and picking either unilaterally would widen scope past what the finding needs. `int-82`'s sibling in PR #258 closes the honesty half meanwhile: the confirm now names the session it would destroy |
| `int-33` | **Fixed for the deck flows** (2026-07-27) | DG-01, DG-03 | reset-deck-progress.md §7; create-deck.md; edit-deck.md; move-deck.md; delete-deck.md; kit `Snackbar`; navigation-overlays.md | `TEST-WBS-3.x-snackbar`; the five deck flow tests | PR #207 | **No flow in the app confirmed that anything succeeded.** Five deck specs name the exact copy — `Deck created` with an `Open` action, `Deck updated`, `Deck moved`, `Deck deleted`, `Deck progress reset` — and none of it existed: `ScaffoldMessenger` appeared nowhere in `lib/`, and the only trace of a snackbar was a doc comment on `MxActionCallout` pointing at an `MxSnackbar` nobody had built. Every dialog closed on success and left the learner to infer the outcome from the screen behind it, which is what a silent failure looks like too. `MxSnackbar` builds the kit component against the sixteen `--memox-snackbar-*` tokens that were already defined and had no consumer, and `showMxSnackbar` puts it above the bottom nav on the gutter. Two behaviours the kit and `navigation-overlays.md` ask for are pinned by tests rather than left to Material's defaults: a second confirmation supersedes the first instead of queueing behind it, and taking the action dismisses the bar before navigating, so the destination is not left with one floating over it. The bar is raised *before* the dialog pops — the messenger is app-level, so it outlives the dialog that asked for it and survives the route change in the delete flow. `Open` needed the created deck's id, which the shared `AsyncValue<void>` runner does not carry; it sits beside the command state rather than widening a contract every command in the app shares. **Outstanding:** the reminder flow (`edit-study-reminder.md`) asks for the same confirmation and has no feature to wire it into yet — no reminder code exists in `lib/`. The flashcard family landed in PR #208 (`int-36`, which also turned up `int-35`) and the study goal in PR #209 (`int-37`) |
| `int-30` | **Fixed** (2026-07-27) | DG-03 | delete-deck.md §1, §4; srs-8-box-policy.md §3; WBS 4.3 deck exclusivity | `TEST-WBS-6.1-impact` | PR #204 | **The delete confirm named two of the three quantities the spec requires.** `delete-deck.md` §1: "Copy nêu số child Deck, card **và learning progress** bị xóa." `DeckDeletionImpact` carried the deck and card counts; the learning progress appeared only as an unquantified phrase — "and their learning progress" — so a learner could not tell a deck they had studied for weeks from one they had merely filled in, on the screen whose whole job is to say how much is about to be lost irreversibly. The impact now counts cards past Box 0. Counting progress *rows* would have restated the card total, since every card gets one at creation (`initialise-card-progress.md` §1); Box 0 means never introduced, so history begins at Box 1 and the count is of cards that actually have some. The suffix is omitted rather than reading "0 of them" when nothing was studied. Four seam tests run the summary and the delete over one store, since a confirm dialog is only worth showing if its numbers are the ones the action will act on. Writing them turned up the deck-exclusivity constraint (WBS 4.3) refusing a fixture that put cards and subdecks in one deck — the schema teaching the rule |
| `int-29` | **Verified** (2026-07-27) | DG-03 | WBS 5.3; manage-flashcard flows | `TEST-WBS-5.3-card-stream` | PR #202 | **The card list inside a deck had the same unobserved reactivity as the deck list** (`int-28`), one level down. Five tests drive the real mutations into a live subscription: create, edit, move (waking **both** decks, as the deck move does), hide and delete. The hide case pins a distinction worth having in a test rather than in a reader's head: hiding keeps the card in the deck list the learner manages it from, and removes it only from the study queues and the mastery denominator. The edit case goes through the optimistic-concurrency guard with the version the list emitted, which is what a screen would hold. All five pass; nothing needed fixing |
| `int-28` | **Verified** (2026-07-27) | DG-03 | WBS 5.2.4A; browse-nested-decks.md | `TEST-WBS-5.2.4A-stream` | PR #201 | **The Library's reactivity was assumed, never observed.** `WatchLibraryUseCase` promises that "creates/renames/moves re-emit through the repository stream"; a mutation that writes correctly but does not wake the stream leaves the Library showing the previous list, and is indistinguishable from one that works — the silent-no-op shape of `int-24`. Five tests now drive the real mutations into a live subscription: create, rename, move (which must wake **two** lists — the deck leaves the root and appears under its new parent), delete, and the direct-cards-only counter rule the reactive read has to honour as much as the one-shot query. **The first cut of this test would have passed either way**, and that is worth recording: it used `stream.first` per step, which cancels its listener so the next call re-subscribes and drift re-runs the query. One listener now stays open for the whole test and the emissions are counted, so asserting the second value asserts that the stream emitted twice. All five pass |
| `int-27` | **Verified** (2026-07-27) | DG-03 | remove-language-pair.md; load-today-dashboard.md §2; select-language-pair | `TEST-WBS-5.7.1-pair-seam` | PR #200 | **The last source feeding the Today projection had never been driven across the boundary.** Every count the dashboard shows is pair-scoped, and the pair comes from a stored preference rather than from anything the projection holds — and that scoping has a history: Today once read an unscoped due count and advertised review work from a library the Library tab does not show. Four tests now drive the real `SelectLanguagePairUseCase` over a real preference store: switching the active pair switches the due count, the mastery denominator and the Recent-decks rows together; no selection counts nothing rather than falling back to everything; a pair still owning decks refuses removal and leaves the dashboard untouched; and removing the active pair clears the selection, so the projection does not keep reading through a dangling id. All four pass. With `int-24`, `int-25` and `int-26` this closes every seam into the Today projection — study, library and language pair |
| `int-26` | **Fixed** (2026-07-27) | DG-03 | handle-caught-up-today.md §1; handle-empty-library-today.md §1; load-today-dashboard.md §2 | `TEST-WBS-5.7.1-mutations` | PR #199 | **Today decided "empty library" by counting decks, not cards** — and held the answer in a variable called `libraryCardCount`, which is how it survived review. `DeckRepository.countForLanguagePair` is documented as "number of decks (any depth)"; the language-pair removal guard is its actual caller. So a deck whose cards had all been deleted reported **caught-up**: the congratulation "You're all caught up — no cards due right now" shown to someone with nothing to study and nothing to review. `handle-caught-up-today.md` §1 forbids exactly that — caught-up "yêu cầu Library có eligible content" — and the empty state is the one that tells a learner how to start again. The decision now reads `LibraryMastery.studiableCount`: cards that are neither deleted nor hidden, which is what "eligible content" means. The mastery read simply moves ahead of the branch; no new query. Found by the same seam method: four tests drive the remaining structural mutations — move a deck under another, move a card between decks, delete a card, delete the last card — and then read the dashboard. The first three passed; the fourth is this defect |
| `int-25` | **Fixed** (2026-07-27) | DG-03, DG-04 | delete-deck.md §1, §3 node G; handle-empty-library-today.md §1; surface-due-cards.md §1 | `TEST-WBS-5.7.1-library-seam` | PRs #197, #198 | **Deck deletion was broken for every deck anyone had actually used.** `DriftDeckRepository.delete` ran a single `DELETE FROM decks WHERE id = ?`; nothing that points at a deck or its cards cascades. A deck holding a card failed on `flashcards.deck_id` (#197), and a deck that had been *studied* failed again on `study_sessions.deck_id`, `study_attempts.card_id` and `learning_progress.last_terminal_attempt_id` (#198) — which is the usual reason to delete a deck at all. Descendants were never removed, against §1's "Parent delete áp dụng toàn descendants". **The existing test asserted the opposite and passed**: it used a fake repository, and its own doc says the use case "delegates the atomic cascade to the store". The store had no cascade to delegate to. Both sides were tested; the seam was never crossed. Delete now removes the whole chain in one transaction — terminal-attempt references cleared, then attempts, sessions, cards and finally the deck subtree — so a failure removes nothing (§1). **What is lost is stated rather than discovered later**: the session history of a deleted deck goes with it; the learner's streak and daily goals do not, because those are keyed by local date in their own tables and never referenced the deck. Five seam tests cover it, including deleting a deck that has been studied end to end |
| `int-24` | **Verified** (2026-07-27) | DG-03, DG-04 | load-today-dashboard.md §2-3; srs-8-box-policy.md §3/§8 | `TEST-WBS-5.7.2-seam` | PR #196 | **The dashboard's supporting sections were tested against rows a test had seeded by hand.** That proves the queries, not that anything produces the rows they read: a Recent-decks list ordered by `last_reviewed_at` is a silent no-op if no study path ever writes that column, and it looks identical to one that works. Four tests now drive real sessions and then ask the projection: studying a deck moves it to the front of Recent decks (so the ordering column is genuinely written, by `updateProgressGuarded` through the terminal-outcome path); the most recently studied deck leads rather than the most recently created; a card promoted out of Box 7 raises the library-mastery stat, which is the only way Box 8 is ever reached; and a session left mid-answer makes Today offer Continue with the due count still whole, because answers are graded at finalize. All four pass — the seam is real, not decorative. Together with `int-22` and `int-23` this closes the study↔dashboard boundary that three of this session's defects lived on |
| `int-23` | **Fixed** (2026-07-27) | DG-03, DG-04 | track-daily-goal.md §1; srs-8-box-policy.md §7/SRS8-011; record-streak-day.md §1 | `TEST-WBS-5.7.4-contrib` | PR #195 | **A retried finalize double-counted the daily goal.** `track-daily-goal.md` §1 is explicit — "Paused/abandoned/retried Finalize không double-count" — and the SRS half honours it on its own, because every terminal attempt carries the stable `terminal:<sessionId>:<cardId>` key (SRS8-011). The **goal bucket did not**: `TrackDailyGoalUseCase` adds the session's qualified cards unconditionally, so a second finalize counted them twice and could mark a goal met that was not. The streak survived only because its unique local date absorbs repeats. **The assumption that hid it is written down in the code**: `TrackDailyGoalUseCase` says its idempotency is "inherited, not re-implemented … the day bucket rides the session-finalize exactly-once contract". That holds only if finalize runs its side effects once, and nothing made it so — finalize re-ran happily over an already-completed session. It now re-reads the session and returns the summary without offering a second contribution when the store already reports a terminal state. **Found by testing the seam, not either side.** Both halves had unit tests against fakes and both passed; the defect only exists where the writer and the reader meet over one real store. That is the same place `int-11` lived, and this round's five round-trip tests now cover it |
| `int-22` | **Verified** (2026-07-27) | DG-03, DG-04 | WBS 5.7.4; srs-8-box-policy.md SRS8-001; load-today-dashboard.md §2 | `TEST-WBS-5.7.4-release` | PR #194 | **Nothing covered the seam between finishing a session and what the dashboard then says.** The five-stage pipeline had a test against a fake repository, and the Today projection had its own; no test crossed between them — which is where every Today defect this session found actually lived, the study half right and the projection disagreeing. The release journey now runs over the real store: a fresh install with five valid cards, a newLearning session through all five stages, finalize, then the Today read that follows. It pins that activation puts every card in Box 1 with a future due date (SRS8-001), that Today therefore reports **caught-up rather than the empty library it started as**, that `newCount` falls to zero, and that the same cards come back as `startReview` with a due count of five once their dates pass. One thing the test makes explicit rather than assuming: caught-up is the honest state for a library of five *unstudied* cards **only because** that surface offers the new-learning action (`int-12`'s sibling, added 2026-07-26). Without it the screen would congratulate someone who has never studied. Both tests pass; nothing needed fixing |
| `int-21` | **Fixed** (2026-07-27) | DG-03, DG-04 | resume-study-session.md §§1, 3 nodes E/H, §4 | `TEST-WBS-5.6.3-resume-error` | PR #192 | **A broken session read as no session.** `LoadStudyRuntimeUseCase` returned `null` both when nothing was active and when an active session's round order could not be read, and the screen renders `null` as the empty "no session" state. §3 separates those: node E is "Missing — session unavailable", node H is a recoverable error with `Try again`. Collapsing them told a learner they had nothing to resume while their committed answers sat in the store, and offered no way to retry — the screen's async builder had an error title and no `onRetry` at all, so even a thrown failure would have been a dead end. `null` now means exactly one thing (nothing to resume); an assembled-but-incomplete session raises `DataCorruptionFailure`, and the screen offers the retry §3 asks for. **`Start fresh` is still not offered**, on the same ground as `int-15`: ending a running session is undefined, and here it would discard answers the learner can still see the count of. The spec pairs Retry with it; only the half whose consequences are defined is built |
| `int-20` | **Verified** (2026-07-27) | DG-03, DG-04 | exit-study-session.md §1; resume-study-session.md §1; WBS 5.6.12 | `TEST-WBS-5.6.12-restart` | PR #191 | **Exit/resume across a real restart had no coverage.** 5.6.12 asks that committed progress survive and that round state come from the committed checkpoint rather than UI memory — the one rule an in-process test cannot honestly check, because anything held in memory would satisfy it by accident. Three tests now run over a file-backed store through `RestartHarness`, so the second half executes on a fresh connection and object graph. They pin: a session left mid-way resumes at the committed position with no attempt re-submitted (`resume-study-session.md` §1); leaving grades nothing, because the SRS grade is aggregated at finalize and exit is not finalize (`exit-study-session.md` §1); and a session finished after a restart finalizes once, grading every card exactly once across both halves. **No product defect found.** The one failure was in the test: its sequential id generator was rebuilt per wiring, so the counter restarted and minted an attempt id the store already held. Production generates UUIDv7 and has no such id space to reuse — the generators are now declared outside the wiring, with the reason written down, because the next restart test will make the same mistake otherwise |
| `int-19` | **Verified** (2026-07-27) | DG-03, DG-04 | relearn-cards.md §§1-3; srs-binary-review.md §2; ST-TYPE-015/016 | `TEST-WBS-5.6.11-relearn` | PR #190 | **The relearn loop had no end-to-end coverage either.** It has been startable since the Study Result's `Review mistakes` was wired, and the unit tests covered the queue-selection rule in isolation, but nothing walked source session → finalize → relearn start → finalize. Five tests now do, over the real repositories and factory. Four rules that only appear across two sessions are pinned: the queue is the finalized summary rather than the scope (§1); the source session keeps its attempts (§1, "Relearn không xóa Attempts trước đó"); a passed relearn promotes from the box the miss demoted the card to, not the original (`srs-binary-review.md` §2, explicitly "không phải undo trong cùng transaction"); and a two-card queue runs the binary plan because it cannot fill a five-option Guess pool (ST-TYPE-015/016). The fifth is a regression guard on the interaction between `int-15` and this flow: the active-session conflict added in PR #186 must not block the very handoff the Study Result offers — finalize releases the slot, so `Review mistakes` has one to take. Writing it also documented a rule that reads like a bug until stated: missing a card on *every* round never completes a session. §2's sticky wrong requires the card to pass a mastery retry round before the session can end, while its terminal grade stays wrong. All five pass; nothing needed fixing |
| `int-18` | **Fixed** (2026-07-27) | DG-03, DG-04 | refresh-today-projections.md §3; srs-binary-review.md §§2, 4; srs-8-box-policy.md §7 | `TEST-WBS-5.6-due-pipeline` | PR #189 | **The due-review loop had no integration test, and finishing one left Today stale.** The first-learning pipeline has had an end-to-end test since 5.7.4 was scoped; due review never got one, which is how its stage reached production with no screen behind it (`int-17`) without a single failure. The new test walks the path a learner walks from Today's `Start review` over the real repositories, factory and advance policy: start over the due queue, answer every card, finalize. It pins the two rules that only appear across a whole session — a `Relearn` card comes back in the mastery retry round and still finalizes as wrong (§2, §4), and a finalize retry re-applies nothing (§7). All four passed first run, so the loop is confirmed working rather than merely reachable. **The stale-dashboard half was real.** `refresh-today-projections.md` §3 lists "Study exit/finalize" among the refresh triggers and only the manual pull was wired, so the dashboard behind a finished session still advertised the review that had only now been completed — with a due count, a goal bucket and a streak the session had all just changed. Both exits from the Study Result now recompose the projection |
| `int-17` | **Fixed** (2026-07-27) | DG-03, DG-04 | srs-binary-review.md §§1-4; ST-TYPE-005/016; start-review-from-today.md | `TEST-WBS-5.6.5-binary` | PR #188 | **The mode every due review runs had no screen.** `study_session_screen.dart` dispatched stages with a `_ =>` wildcard arm rendering "coming soon", and `StudyModeType.srsBinaryReview` fell into it — so a `dueReview` session could be started and never answered. The domain half was complete: the strategy, its canonical mapping and the factory resolution all existed and were tested. Only the surface was missing, which is why nothing failed. **This became reachable in PR #181**, when Today's `Start review` was wired to actually start a due-review session: the handoff worked, and led to a dead end. That is the second time a green build hid a screen that renders nothing — the first was `MX-VIS-049` passing at 1.19% with four kit controls absent. The screen is built to §1: term *and* meaning shown, two actions, no reveal step and no timer — this mode does not test recall, it asks the learner to grade themselves against the answer in front of them. Each action carries its own glyph (§4's non-color affordance) and its own attempt identity, because §3 makes the same identity with a different action a conflict. **The wildcard is gone**: the dispatch switch is exhaustive over `StudyModeType`, so the next mode added to the enum is a compile error rather than a screen that quietly says "coming soon". The unused string was deleted, and the l10n usage guard is what noticed it |
| `int-16` | **Fixed** (2026-07-27) | DG-03, DG-04 | resume-study-session.md §1; ST-TYPE-015/016/018; session_mode_plan.dart | `TEST-WBS-5.6.3-plan` | PR #187 | **Resuming a Relearn session silently changed its mode plan.** `LoadStudyRuntimeUseCase` rebuilt the plan with `SessionModePlanResolver.resolve(type:)` and no `guessPoolSufficient`, so the parameter took its `false` default and the Relearn branch always returned the binary fallback. A session started under the Guess plan (ST-TYPE-015, five distinct meanings in the pool) therefore came back as `srsBinaryReview` — a different plan from the one the learner's answers were being graded under, and the exact thing `SessionModePlan` documents as forbidden: "Once snapshotted the plan is replayed verbatim on Retry/Resume — never re-resolved (ST-TYPE-018)". **A stale comment was the guard.** The use case said the shortcut was safe because "practice/relearn are not startable yet"; relearn became startable when the Study Result's `Review mistakes` was wired, and nothing revisited the comment. The plan is now replayed from the snapshot: `study_sessions` stores no plan id, so the branch input is recomputed over the **frozen card snapshot rows**, which is a replay of the start-time computation rather than a fresh question to today's library. Two tests pin the branch in both directions so the fix cannot be a hard-coded Guess. **Follow-up:** the snapshot ought to carry its plan id — `start-study-session.md` §1 lists "mode plan" among what the snapshot fixes, and schema v1 has no column for it. Recomputation is faithful only while the inputs stay derivable |
| `int-15` | **Fixed** (2026-07-27) | DG-03, DG-04 | start-study-session.md §1, §3 node C/D, §4; exit-study-session.md §1; track-daily-goal.md §1 | `TEST-WBS-5.6.2-active` | PR #186 | **Starting a second session was left to a database constraint.** `StartStudySessionUseCase` never checked for an active session; the partial unique index on `state = 'active'` stopped the write, so a learner tapping a deck's study bolt mid-session got a raw constraint violation. §3 node C is an explicit revalidation and §1 requires a choice — "không ghi đè im lặng". A constraint can stop a write; it cannot say what to do instead. The check is now in the flow and raises `ConflictFailure(code: 'active-session')`, which the deck row turns into the kit's `Continue your session?` prompt. **`Start over` is not offered**, and that is the finding worth recording: no document owns what ending a running session means. `exit-study-session.md` §1 says leaving *pauses* and that the default confirm carries no destructive discard; `track-daily-goal.md` §1 mentions an abandoned finalize only to say it must not double-count. Nothing states whether an abandoned session's committed answers keep their goal and streak contributions, so the button's consequences are undefined. Needs an owner decision. **A second defect fell out of building it:** the check first read `watchActive().first`, which opens a query stream and waits for an emission that arrives a frame later — the deck-detail test caught a start silently not happening. The port gained `activeSession()`, a point-in-time read, and the three other callers that were asking a stream for a value were moved onto it |
| `int-14` | **Fixed** (2026-07-27) | DG-01 | WBS 2.10 foundation responsive snapshots; kit `dashboard/greeting` | `TEST-WBS-2.10-01` | PR #185 | **The foundation goldens depended on the wall clock.** Today greets by time of day, so once the greeting landed the snapshots recorded whatever hour they were taken in — they held `Good evening` and began failing after midnight, rendering `Good morning`. Every developer would have hit it at a different hour and re-recorded the baseline against their own, so the file would have flipped back and forth forever. The test now pins `appClockProvider` and `appTimeZoneProvider` — the same two determinism seams the parity build pins — to the instant the existing baseline was taken at, so the recorded images did not need to change. **Why it matters:** a golden that changes with the time of day is worse than no golden: it trains everyone to re-record on failure, which is exactly how a real regression gets waved through |
| `int-13` | **Fixed** (2026-07-26) | DG-03 | refresh-today-projections.md §§1, 4, 6; WBS 3.9 shared async renderer | `TEST-WBS-3.9-stale` | PR #183 | **A failed refresh threw away the dashboard it was refreshing.** `MxAsyncBuilder` documented a "retained-composition contract" and implemented only half of it: `skipLoadingOnRefresh` keeps data while a refresh is in flight, but `AsyncValue.when` has no error counterpart, so a refresh that *failed* replaced a screenful of real data with an error banner. `refresh-today-projections.md` §1 is explicit — "Refresh failure giữ last-good Dashboard với stale marker" — and §6 repeats it. The retained-error path is now handled before `when`, behind an opt-in `staleLabel`: a screen whose provider only ever loads once keeps the error surface, which is correct for it, and cannot silently start showing old data instead. **Why it matters:** this is the failure mode a user meets on a flaky connection, and the old behaviour lost the only data they had — for a local-first app whose data never left the device. Today opts in and now also offers pull-to-refresh, §3's one reachable trigger |
| `int-12` | **Open — two Today stats have no source** (2026-07-26) | DG-03 | kit `dashboard/today` Stat strip; metric-formulas-v1 `activeStudyDurationMs`; track-daily-goal.md §1 | — | PR #179 | **The kit's Today strip shows four metrics; two of them cannot be read from anything this app records.** Day streak and library mastered are built and true. The other two are not: *minutes studied* is `int-9` — a specified formula with no producer anywhere, so any number would be invented; *words learned today* is recorded only for learners who configured a daily goal, and for those who have one the Daily-goal card directly above already states it. **Re-audited 2026-07-27, and the reason is stronger than this row said.** It read as though `TrackDailyGoalUseCase`'s early return were the only obstacle — implying a small fix. It is not. `track-daily-goal.md` §1 explicitly *permits* the history — \"Goal disabled vẫn có thể giữ activity history nhưng không tính active attainment\" — so the flow is on the side of showing this. The schema is what forbids it: `goal_day_progress.goal_id` is `NOT NULL REFERENCES daily_goals (id)` and `target_snapshot` is `NOT NULL`, so the bucket is structurally a *goal* bucket and cannot exist for a learner who has never set one. Recording activity without a goal needs a schema change — a nullable goal/target, or a separate activity table — which is the same shape of decision as `int-34` and equally the owner's. The strip therefore ships with two stats rather than four. **Why it matters:** this is the third surface to hit the same missing producer, and the shape of the fix is a product decision no doc owns — where foreground-active intervals are captured, and whether qualified-card counting should be recorded independently of goal configuration (the same conflation `int-11` fixed for the streak). Until then, the honest strip is the short one: a plausible-looking invented number is indistinguishable from a measured one |
| `int-11` | **Fixed** (2026-07-26) | DG-03 | record-streak-day.md §6; calculate-current-streak.md §3; kit `dashboard--not-studied` / `--streak-reset` | `TEST-WBS-5.7.2-note` | PR #177 | **Today threw the streak away whenever no daily goal was configured.** `LoadDailyProgressUseCase` returned `DailyProgressStatus.none()` the moment `latestGoal()` came back null or disabled, before it ever read the day records — so a learner nine days into a run who had never set a target was shown a streak of zero. The two concepts are independent by contract: `record-streak-day.md` §6 says qualification does "không phụ thuộc Daily Goal enabled/met", and `RecordStreakDayUseCase` writes the day on any qualifying session. The projection now runs first and the goal only decides whether the card appears. **Why it matters:** the streak is the retention mechanic the whole dashboard is built around, and a goal is optional — the state where the bug bites is the default one for anyone who skipped Settings. It was also asserted as correct: `no goal configured means no card` expected `streakDays == 0` with a qualified day on record, so the test read as the specification. The same read now also carries `studiedToday` and `hasStreakHistory`, which is what lets the reset note tell a broken run from one that never started |
| `int-9` | **Open — metric with no producer** (2026-07-26) | DG-03 | study-statistics/metric-formulas-v1.md `activeStudyDurationMs`; kit `study-result` time stat | — | (audit, no PR) | **`durationActiveMs` has a specified formula and no input anywhere.** `metrics-v1` defines it as "sum validated foreground-active intervals, excluding background, interruption, loading and clock-negative/overlapping duplicate intervals" — an interval union, not a sum. But nothing produces those intervals: `study_attempts` has no timing column, `CanonicalModeEvidence` carries none, and no flow in `docs/business/**` owns capturing them. The one timer that exists (`RecallTimer`) is a per-card countdown for one mode, in-session and explicitly non-persistent. So the Study Result's 6:30 stat is still supplied only by the `MX-VIS-054` override, the same shape of gap `int-8` recorded for the streak card. Deliberately **not** built: the aggregation policy would be a consumer with no producer, and authoring an interval-capture flow is product no doc owns. Needs an owner decision on where foreground-active intervals are captured and persisted before either half is worth writing |
| `int-8` | **Open — streak/goal never computed** (2026-07-26) | DG-03 | study-streak/calculate-current-streak.md §§1,3,4,6; study-streak/README.md; kit `study-result` streak card | `TEST-WBS-streak-int8-01` | (this PR, policy only) | **The Study Result screen has shipped a streak + daily-goal card since it was built, and nothing in production ever computed one.** `StudyResultGoalStatus` was constructed in exactly one place in the whole tree: the `MX-VIS-054` parity override. So `goalStatus` is null in the real app and the card never renders — while `MX-VIS-054` passes at 1.76% on injected data. That is worth naming as a class: **a parity state seeded through a provider override proves the composition, not that the product can reach it.** The four other overrides in `parity_overrides.dart` pin a *failure* or an in-flight write on a real journey; this one supplied the content itself. `StreakProjectionPolicy` now derives current/longest from qualified-day records per the flow, with §5's state matrix as its test list. `record-streak-day.md` is now built too: qualification per `metrics-v1` (finalized `newLearning`/`dueReview`/`relearn` with at least one qualified Card — Practice never contributes), the local day resolved through a new `AppTimeZone` port rather than the UTC date, and the write offered *after* the session commits inside a `try` so a streak-store failure costs a streak day (rebuildable by reconciliation) and never the finished session (not rebuildable) — §1's "Ghi streak không được rollback Study Session đã thành công", mutation-tested. **Correction to the entry above:** the grace length was recorded here as unspecified. It is specified — `metrics-v1` says "consecutive qualified local-day ids ending today, or ending yesterday when today has no qualifying event yet. A gap before yesterday yields zero." The value implemented was already right; the claim that no doc stated it was not, because the number lives in the statistics formulas rather than in the streak flow. `track-daily-goal.md` is built too, with the crossing that `complete-daily-goal.md` consumes derived from the bucket rather than from an emitted-events table — a day already met reports no further crossing however many sessions land on it. Contribution idempotency is inherited from the session-finalize exactly-once contract (schema-v1 atomic operation 5), which is why there is no contribution ledger. Both projections are wired into finalize, each in its own `try`, so neither can fail the session nor each other. **Unit corrected:** `StudyResultGoalStatus` carried `goalDoneMinutes`/`goalTargetMinutes` and the screen rendered "14/20 min", which matched neither `metrics-v1` ("Daily Goal unit v1 is qualified Cards") nor the schema (`target_card_count`, `qualified_card_count`). The kit shot reads "min", but its own `StreakGoalCard.d.ts` records those numbers as hardcoded samples that "Flutter parameterizes" — so the shot never fixed the unit. Now cards throughout; `MX-VIS-054` costs 0.02pp for the wider word (1.76 -> 1.78 light) and still passes. Finalize now reads both projections back and attaches a real `StudyResultGoalStatus` to the summary: the streak is derived from the day records (nothing stores a running total to drift) and the goal numbers come from the bucket the session just contributed to. Null when no goal is configured — the card shows today's target, so with no goal there is nothing to show. Covered end to end in the finalize test. **Correction to this row's own plan:** it said the `MX-VIS-054` override could then be deleted. It cannot, and the reason is worth keeping rather than rediscovering — the Study Result needs a *finalized* session, which is not an active row any resume journey reaches, and reproducing the shot would mean answering 24 cards through Playwright to hit 24 / 88% / 6:30. The override's justification (a terminal state no journey can enter) was always the sound half; what was wrong was that it also invented `goalStatus`, which production could not build at all. That half is closed. **Still open:** `durationActiveMs` is documented as "0 until that timing evidence is summed", so the result's 6:30 time stat is still supplied only by the override — the same class of gap as the streak card was, one field over |
| `int-7` | **Closed by this PR** (2026-07-26) | DG-01 | hide-flashcard.md §1; kit `flashcard-editor/more-toggle` + `flashcard-editor/visibility`; core kit `MxSwitch` | `TEST-WBS-6.5-int7-01` | (this PR) | **The editor had no advanced disclosure, so the built hide feature was unreachable from it** — column, query, repository, use case, viewmodel and every due/study query excluding hidden cards all existed, and even the `moreOptionsLabel` string sat unused in the ARB. Building it surfaced two further defects. (a) `insertFlashcard` never listed `is_hidden`, so every created card took the schema default: a card created hidden was stored visible. Invisible until now because create hard-coded `isHidden: false`; the value is now applied inside the same transaction rather than widened into an insert with ~50 positional call sites. (b) The editor reads its card through a *one-shot* provider, not the stream the deck list uses, so a toggle wrote through but the switch snapped back; the displayed state is now local and seeded from the card, because re-reading would rebuild the form and discard a part-typed term/meaning. Three shared widgets landed with it: `MxSwitch` (core kit component, referenced by flashcard-editor/reminder/settings and never built), `MxDisclosure` and `MxSettingRow` |
| `int-6` | **Closed by this PR** (2026-07-26) | DG-01 | manage-card-translations.md §2 entry points; kit `flashcard-editor/add-translation` | `TEST-WBS-6.4-int6-01` | (this PR) | **The add-translation entry point was gated on edit mode, so a card being created had no route to a translation at all** — the learner had to save the card, leave, reopen it and edit. Both sources say otherwise: `FlashcardEditor.jsx` hangs the `+` off the Meaning label with no mode gate, and the business doc lists the entry points as "Create/Edit Card -> Add translation". The parity ratio could not see it: a missing 24px glyph is far below the 3% gate, and `MX-VIS-049` passed at 1.19% without it. Found by reading the render against the kit shot rather than by measuring — the ratio says how *much* differs, never *what*. Create now carries a draft slot whose text goes through `CreateFlashcardUseCase.translations`, so the translation commits in the same transaction as the card; a follow-up write would have left a stored card without its translation and made the retry create a duplicate card |
| `int-5` | **Open — shared study shell; re-verified 2026-07-27** (2026-07-26) | DG-01 | kit `match-mode`, `fill-mode`, `review-mode`, `guess-mode`, `recall-mode` app bars | — | (audit, `tool/parity/structure_report.mjs`) | **Every study screen's app bar diverges from the kit in the same two ways, and no ratio catches it.** The kit draws a bare `arrow_back` and an `actions={MxIconButton more_vert}` overflow; this build renders no overflow action at all. **The "circular chip" originally recorded here was wrong** and is struck: the arrow was never chipped, it was `MxTappable`'s hover state, because Playwright's pointer starts at (0,0) — over the back button. Fixed in the harness (`expectStableCapture` now parks the pointer), not in the widget. Confirmed on `match-mode--playing` and `fill-mode--waiting`; the shell is shared, so the other three inherit it. Found by ink-profile correlation, not by the gate: `MX-VIS-053` passes at 1.97% with a **row correlation of 0.385**, and `MX-VIS-062` at 1.43% with **0.659 column**. Fixing the chip is styling; the overflow needs its menu contents decided, which is product — hence recorded rather than built. **Re-verified 2026-07-27 against the documents rather than from memory:** nothing in `docs/business/study-session/**`, `study-mode/**`, `preferences/**` or the kit's own `specs/**` names a single entry for it. Building the button anyway would put a control on screen that opens nothing, which is the pattern this register rejects elsewhere (`int-79`'s Practice path, the Today create sheet's absent Import row). Genuinely blocked. **Closed 2026-07-26 for the labels:** Fill's prompt and placeholder are deck-driven via `studyLanguageContextProvider`, which resolves the session deck's pair the way `cardEditorContext` already did for the editor — `StudyRuntimeState` carries frozen card text and no language identity, so the names cannot come from the snapshot. An unresolved pair falls back to the unqualified copy rather than rendering "Type the term ()"; both paths are tested. **Note the kit disagrees with itself on the name form:** `fill-mode--waiting` writes the English exonym ("Korean") while `flashcard-editor--create` writes the native name ("Term · 한국어"). The app uses `nativeName` throughout rather than matching each shot, which costs `MX-VIS-053` 0.11pp. **Still open:** the `more_vert` overflow action, whose menu contents are product. **Closed 2026-07-26 for the tile type role:** the kit's Match tile is `font-weight-bold` at base size against this build's `MxTextRole.body` (base/400). The scale had no base+bold step, so three call sites had borrowed `button` — a label role that happens to share the pairing. `MxTextRole.bodyStrong` and `captionStrong` now carry it, and the tile uses the former. Note this moved the Match ratios *up* ~0.04pp rather than down: the tiles now match the kit's weight exactly, and the residual is the meanings column order (kit `LEFT = time, love, friend, food, school`; ours is a different shuffle), which bold glyphs disagree on more loudly than regular ones did. The JSX rules, so the weight stays — so closing it means either adding a role to the shared type scale or giving the tile a local style, and widening the shared API for one feature-local tile is a call worth making deliberately. The tile's *alignment* was fixed at the same time (the kit centres on both axes; this build left-aligned), which moved the ratio 0.00pp |
| `int-4` | **Open — status correction** (2026-07-26) | DG-03 | ADR-003; `business/study-mode/**`; the `5.5.1`–`5.5.6` boundaries | — | (audit, no PR) | **The whole `5.5` Study Mode stream is built and tested while the plan still records it Blocked.** Verified row by row rather than inferred from filenames: `5.5.1` — `StudyModeType` carries all six modes and `canonical_mode_evidence`/`study_mode_input`/`mode_outcome` import nothing outside `domain/` or `dart:`, so the no-UI/no-data clause holds; `5.5.2` — `study_mode_strategy_base` with a validate→evaluate→map template test; `5.5.3` — `round_order_policy` with its seed/collision suite; `5.5.4` — six concrete strategies under `strategies/`; `5.5.5` — `StudyModeFactory.standard()` injected by `studyModeFactoryProvider`, resolving every mode, returning the same instance per id and failing fast on a missing or duplicate registration; `5.5.6` — every case the boundary names is covered by name: Guess five-options, Recall's closed 20s deadline, binary self-grade before reveal, Fill case/alternative/diacritic normalisation, Match pair-id classification. **68 tests pass** across `test/domain/study_modes/`. No status is flipped to Done here: §6.2 requires an item packet before promotion and no `WBS-5.5.*` packet exists, so the sign-off is the owner's. What is corrected is the claim that the work is Blocked |
| `int-3` | **Open — needs an owning row** (2026-07-26) | DG-01 | create-deck.md §8/§10/§18; kit `create-deck-dialog` (14 states) | — | (audit, no PR) | **The Create Deck *dialog* — the second-deck-onward surface — has no WBS row, and eight census rows point at one that is Done.** `MX-VIS-025`–`033` all record "blocked on `5.2.4`", but `5.2.4` is *Library and open Deck* and is listed Done; the dialog's fields belong to none of `5.2.1`–`5.2.6` (`5.2.2` is the create transaction, `5.2.3` is the first-run two-step, a different surface with its own `create-deck-firstrun` shots). A reader following those rows concludes either that the states are ready or that `5.2.4` is incomplete; neither is true. **Measured gap:** the kit draws name + language pair (a picker at root, read-only "Inside <parent>" when nested) + an optional description behind progressive disclosure, one primary CTA, a form that locks whole while submitting, and a dirty-dismiss discard confirm — across root-default / root-multi-pair / root-missing-pair / nested / optional-expanded / validation / duplicate-root / duplicate-sibling / name-too-long / submitting / submit-failure / long-name / keyboard-open / discard-confirm. This build's `create_deck_dialog.dart` is 136 lines: an "Inside X" caption, **one** name field, an error line and Cancel/Create. Sizing it is the owner's call — it is XL by the kit's own state count — so no row is minted here; the eight census rows are re-pointed at this entry instead of at a Done row |
| `int-2` | **Closed by `5.4.2`** (2026-07-25) | DG-01 | Onboarding-slice completion; kit `library--loaded` chrome | — | (PR #98, `1a4e1ea`/`17b51a4`/`32984ad`/`4a84e48`) | Library kit-alignment landed ahead of its owning WBS rows and is recorded here rather than promoting those rows: **FilterRow** (scope / filters / sort) with a new shared `MxChip` — chip is a `3.8` primitive, the row itself is `10.2` chrome; **FAB + A-Z sort**; **per-deck due/new status** on `MxDeckCard` via `watchRootDeckSummaries` — a `5.4.2` read model. **Defect (2026-07-22 audit): both status counters are structurally unreachable in production.** `decks.drift` derives `new_count` from `learning_progress.id IS NULL`, but `drift_flashcard_repository.dart:70` inserts a Box-0 progress row inside the same create transaction for every card, so `new_count` is always 0; `due_count` needs `due_at <= now`, and the only writer of `due_at` is the SRS policy from `5.4.3`/`5.4.4`, which is not built, so it is also always 0. Every deck therefore renders "up to date". The two tests that cover the counters (`library_screen_test.dart:109`, `:134`) seed `flashcardDao`/`learningProgressDao` directly and never exercise the production write path, so they pass against data the app cannot produce. Owning rows: `5.4.1` (New state — should derive from `box = 0`, not from a missing row) and `5.4.2` (due/new queue policy). Blocked `MX-VIS-019`/`020`/`023` regardless of the FilterRow work. **Resolved 2026-07-25 by `5.4.2`:** the query reads New as Box 0, and the covering test now builds its cards through `CreateFlashcardUseCase`. Those three `MX-VIS-*` rows stay Blocked on their remaining chrome (FilterRow skeleton, app-bar search/avatar), not on the counters |
| `3.10` | Done | DG-02, DG-05 | Kit _shared ConfirmDialog/SelectSheet specs | `AC-WBS-3.10-01`; `TEST-WBS-3.10-01` | `docs/wbs/implementation-packets/WBS-3.10-shared-composites.md` | `mx_confirm_dialog.dart`, `mx_select_sheet.dart` + 4 tests (2026-07-19); callout/breadcrumb/study-prompt boundary recorded (land with first consumers); full-gate pass marker |
| `3.12` | Done | Gate PASSED | WBS 3.12 minimal-journey scope; 3.x suites | `AC-WBS-3.12-01`; `TEST-WBS-3.12-01` | `docs/wbs/implementation-packets/WBS-3.12-minimal-mx-gate.md` | Journey→component matrix verified: docs contract, widget tests, kit-width goldens, semantics; 3.8 unused-by-journey exception recorded (2026-07-19) |
| `3.15` | In progress (A Done) — **re-scoped to inner-loop layer** | Owner rule ≤3% | Kit shot matrix (240 states ×2 themes @390) | `AC-WBS-3.15-01`; `TEST-WBS-3.15-01` | `docs/wbs/implementation-packets/WBS-3.15-kit-parity-gate.md` | Harness (`test/support/kit_parity.dart`, real-font 2× pixel diff) + 4 enforced states (landing 1.42%, library-empty 1.10%, step1, step2). Merge-gate authority transferred to Phase 0 (Flutter Web + Playwright); 3.15 remains pre-push fast feedback and its enforced states migrate into the `MX-VIS-*` register (2026-07-19) |
| `3.9` | Done | DG-02 | Guard-named async APIs, ADR-007, failure taxonomy | `AC-WBS-3.9-01`; `TEST-WBS-3.9-01` | `docs/wbs/implementation-packets/WBS-3.9-async-infrastructure.md` | `shared/viewmodels/{mx_async_draft,mx_action_runner,mx_action_errors,mx_async_builder}.dart` + 10 tests (2026-07-19); full-gate pass marker |
| `3.7` | Done | DG-02, DG-05 | Kit feedback prompts + CSS | `AC-WBS-3.7-01`; `TEST-WBS-3.7-01` | `docs/wbs/implementation-packets/WBS-3.7-feedback-primitives.md` | `mx_progress`, `mx_banner`, `dialogs/mx_dialog`+helper, `bottom_sheets/mx_sheet`+helper, `sheetTop` radius + 8 tests (2026-07-19); full-gate pass marker |
| `3.6` | Done | DG-02, DG-05 | Kit nav prompts + CSS, toolbar guard rule | `AC-WBS-3.6-01`; `TEST-WBS-3.6-01` | `docs/wbs/implementation-packets/WBS-3.6-navigation-primitives.md` | `mx_{icon_button,fab,bottom_nav,contextual_app_bar,search_dock}.dart` + navLabel role + 12 tests (2026-07-19); collapse/rail boundaries recorded; full-gate pass marker |
| `3.5` | Done | DG-02, DG-05 | Kit MxScaffold contract, screen-shell guard rules, responsive contract | `AC-WBS-3.5-01`; `TEST-WBS-3.5-01` | `docs/wbs/implementation-packets/WBS-3.5-scaffold-shells.md` | Children A–C (2026-07-19): `layouts/**` (content shell, root frame, list/form/study shells) + stress matrix (RTL/safe-area/short-viewport/retained composition); full-gate pass marker |
| `3.4` | Done | DG-02, DG-05 | Kit MxCard/MxList/MxIconTile/MxSectionHeader contracts + CSS | `AC-WBS-3.4-01`; `TEST-WBS-3.4-01` | `docs/wbs/implementation-packets/WBS-3.4-card-list-surfaces.md` | Children A–C (2026-07-19): `mx_card`, `mx_list`, `mx_icon_tile`, `mx_section_header`, `mx_divider` + `sectionTitle` role + tap-matrix test; full-gate pass marker |
| `3.3` | Done | DG-02, DG-05 | Kit MxTextField contract + `.field`/search-dock CSS, hooks guard contract | `AC-WBS-3.3-01`; `TEST-WBS-3.3-01` | `docs/wbs/implementation-packets/WBS-3.3-mx-text-field.md` | Children A–C (2026-07-19): `inputs/{mx_text_field,mx_search_field}.dart`, `shared/hooks/**`, stress suite (long-text/CJK/autofill/200%); full-gate pass marker |
| `3.2` | Done | DG-02, DG-05 | Kit MxButton contract + `.btn` CSS, 3.1 primitives | `AC-WBS-3.2-01`; `TEST-WBS-3.2-01` | `docs/wbs/implementation-packets/WBS-3.2-mx-button.md` | `mx_button.dart`, button text roles, 10 widget tests (2026-07-19); loading maps to kit parent-driven pattern (recorded); full-gate pass marker |
| `3.1` | Done | DG-02, DG-05 | Wave-2 layers, shared-widget guard contracts, `material_symbols_icons` evidence | `AC-WBS-3.1-01`; `TEST-WBS-3.1-01` | `docs/wbs/implementation-packets/WBS-3.1-shared-foundation.md` | `lib/presentation/shared/widgets/{mx_text,mx_icon,mx_tappable,mx_gap}.dart`, `app_border_radii`, `string_utils`, 9 widget tests (2026-07-19); full-gate pass marker |
| `2.10` | Done | DG-02, DG-05 | All 2.x deliveries, token manifest, §5.3 width matrix | `AC-WBS-2.10-01`; `TEST-WBS-2.10-01` | `docs/wbs/implementation-packets/WBS-2.10-foundation-contract-tests.md` | Coverage pin (207), completeness, raw-value source scans, 10 responsive goldens (2026-07-19); full-gate pass marker |
| `2.9` | Done | DG-02, DG-05 | Kit `high-contrast.css`, v1 scope exclusions (0.6, SCOPE.md) | `AC-WBS-2.9-01`; `TEST-WBS-2.9-01` | `docs/wbs/implementation-packets/WBS-2.9-high-contrast-readiness.md` | Readiness delivered + runtime support explicitly deferred (no false claim): `lib/core/theme/tokens/app_high_contrast_overrides.dart`, parity/contrast tests (2026-07-19); full-gate pass marker |
| `2.8` | Done | DG-02 | WBS §5.3 width contract, kit layout roles, ADR-002 | `AC-WBS-2.8-01`; `TEST-WBS-2.8-01` | `docs/wbs/implementation-packets/WBS-2.8-responsive-foundation.md` | Children A–C (2026-07-19): `lib/core/theme/responsive/**` — classes, adaptive values, pane rules, `context.component`, resize tests; full-gate pass marker |
| `2.7` | Done | DG-02 | Extension layer 2.6, kit component/system-UI contracts | `AC-WBS-2.7-01`; `TEST-WBS-2.7-01` | `docs/wbs/implementation-packets/WBS-2.7-theme-assembly.md` | `lib/core/theme/app_theme.dart`, app wiring, `test/core/theme/app_theme_test.dart` (2026-07-19); full-gate pass marker |
| `2.6` | Done | DG-02 | Token layer 2.2–2.5, kit type-scale guideline, theme guard rules | `AC-WBS-2.6-01`; `TEST-WBS-2.6-01` | `docs/wbs/implementation-packets/WBS-2.6-theme-extensions.md` | `lib/core/theme/extensions/**`, `test/core/theme/theme_extensions_test.dart` (2026-07-19); full-gate pass marker |
| `2.5` | Done | DG-02 | Kit `motion.css`/`icon-size.css`, token manifest (14 tokens) | `AC-WBS-2.5-01`; `TEST-WBS-2.5-01` | `docs/wbs/implementation-packets/WBS-2.5-motion-icon-tokens.md` | `lib/core/theme/tokens/{app_motion,app_icon_sizes}.dart`, parity tests (2026-07-19); full-gate pass marker |
| `2.4` | Done | DG-02 | Kit dimension CSS ×6, token manifest (91 tokens) | `AC-WBS-2.4-01`; `TEST-WBS-2.4-01` | `docs/wbs/implementation-packets/WBS-2.4-dimension-tokens.md` | `lib/core/theme/tokens/{app_spacing,app_sizes,app_radii,app_strokes,app_component_dimensions,app_elevations}.dart`, parity tests (2026-07-19); full-gate pass marker |
| `2.3` | Done | DG-02, DG-05 | Kit `typography.css` + bundled variable font, token manifest (27 tokens) | `AC-WBS-2.3-01`; `TEST-WBS-2.3-01` | `docs/wbs/implementation-packets/WBS-2.3-typography-tokens.md` | `assets/fonts/**`, `lib/core/theme/tokens/app_typography.dart`, parity/render tests (2026-07-19); full-gate pass marker |
| `2.2` | Done | DG-02, DG-05 | Kit `colors.css`/`opacity.css`, token manifest (75 tokens), theme-layer guard scope | `AC-WBS-2.2-01`; `TEST-WBS-2.2-01` | `docs/wbs/implementation-packets/WBS-2.2-color-opacity-tokens.md` | `lib/core/theme/tokens/app_colors.dart` + `app_opacities.dart` (generated), CSS-parity + contrast tests `test/core/theme/**` (2026-07-19); full-gate pass marker |
| `2.1` | Done | DG-02, DG-05 | Kit `tokens/*.css`, frozen-name contract, verifier | `AC-WBS-2.1-01`; `TEST-WBS-2.1-01` | `docs/wbs/implementation-packets/WBS-2.1-token-manifest.md` | `docs/design/token-manifest.json` (207 tokens), `tool/design/token_manifest.mjs`, verifier step `design token manifest` (2026-07-19); rename negative-evidence in packet PR |
| `1.9` | Done | DG-05 | ADR-002 locale scope, i18n guard rules, existing ARB pipeline, `intl` baseline | `AC-WBS-1.9-01`; `TEST-WBS-1.9-01` | `docs/wbs/implementation-packets/WBS-1.9-localization-foundation.md` | `lib/core/utils/locale_formats.dart`, plural ARB, `test/support/{localized_app,l10n_fixtures}.dart`, `test/l10n/**` (2026-07-19); full-gate pass marker |
| `1.6` | Done | DG-03 | ADR-003 deterministic contract, Drift time/ID contract, `clock`/`uuid` baseline | `AC-WBS-1.6-01`; `TEST-WBS-1.6-01` | `docs/wbs/implementation-packets/WBS-1.6-deterministic-infrastructure.md` | `lib/core/{time,ids,random}/**`, `test/core/**`, `test/support/fake_clock.dart` (2026-07-19); PRNG known-answer lock; full-gate pass marker |
| `1.7` | Done | — | WBS 1.7 fixture states, migration-policy fixture list | `AC-WBS-1.7-01`; `TEST-WBS-1.7-01` | `docs/wbs/implementation-packets/WBS-1.7-dev-fixtures.md` | `lib/data/dev/dev_fixtures.dart` (6 states, deterministic ids, release-mode guard) + 7 tests (2026-07-19); CLI entry boundary recorded |
| `1.8` | Done | — | Guard warning inventory; profiles.yaml | `AC-WBS-1.8-01`; `TEST-WBS-1.8-01` | `docs/wbs/implementation-packets/WBS-1.8-guard-profile.md` | 98→79 warnings: zero `memox.*` warnings, all real-counterpart stale paths fixed; ci flip criteria recorded in profiles.yaml (blocked on future-path meta-diagnostics) (2026-07-19) |
| `1.10` | Done | — | WBS 1.10 harness list; injectable ports | `AC-WBS-1.10-01`; `TEST-WBS-1.10-01` | `docs/wbs/implementation-packets/WBS-1.10-test-infrastructure.md` | `test/support/{sequential_ids,test_container,database_contract,restart_harness}.dart` + self-proving suite; real-repo-over-memory fixture policy recorded (2026-07-19) |
| `1.11` | Done | ADR-005 | Guard state_management rules; 3.5 command layer | `AC-WBS-1.11-01`; `TEST-WBS-1.11-01` | `docs/wbs/implementation-packets/WBS-1.11-riverpod-foundation.md` | Accepted `docs/architecture/riverpod-foundation.md` + `lib/app/di/core_providers.dart` (clock/ids, keepAlive) + 4 lifecycle/override/invalidation/retry contract tests (2026-07-19) |
| `1.5` | Done | DG-02 | ADR-005, observability/error-handling guard rules, WBS 1.3 reporter seam | `AC-WBS-1.5-01`; `TEST-WBS-1.5-01` | `docs/wbs/implementation-packets/WBS-1.5-error-observability.md` | `lib/core/errors/**`, `lib/core/logging/**`, bootstrap rewire, `test/core/**` (2026-07-19); full-gate pass marker |
| `1.4` | Done | DG-02 | ADR-006, routing guard rules, `go_router` baseline | `AC-WBS-1.4-01`; `TEST-WBS-1.4-01` | `docs/wbs/implementation-packets/WBS-1.4-router-skeleton.md` | `lib/app/router/**`, `test/app/router/**` (2026-07-19); full-gate pass marker `.dart_tool/memox_verify_pass.json` |
| `1.3` | Done | DG-02 | ADR-005, ADR-007, observability guard rules, current `main.dart` | `AC-WBS-1.3-01`; `TEST-WBS-1.3-01` | `docs/wbs/implementation-packets/WBS-1.3-app-bootstrap.md` | `lib/app/bootstrap/app_bootstrap.dart`, `lib/app/app.dart`, `test/app/**` (2026-07-19); full-gate pass marker `.dart_tool/memox_verify_pass.json` |

## Accepted decision gates

| Gate | ADR | Status | Owner/date |
| --- | --- | --- | --- |
| DG-01 | ADR-001 | Accepted | Product Owner, 2026-07-18 |
| DG-02 | ADR-002 | Accepted | Product Owner, 2026-07-18 |
| DG-03 | ADR-003 | Accepted | Product Owner, 2026-07-18 |
| DG-04 | ADR-003 plus frozen canonical SRS path | Accepted | Architecture, 2026-07-18 |
| DG-05 | ADR-002 | Accepted | Product Owner, 2026-07-18 |
| DG-06 | ADR-004 | Accepted | Product Owner, 2026-07-18 |

## First-learning trace

| Capability/output | WBS | Business source | Design source | Architecture/data | Acceptance/test |
| --- | --- | --- | --- | --- | --- |
| Foundation ready | `0.5`, `1.1–1.11`, `2.1–2.10`, `4.1–4.10` | Cross-cutting | Tokens/KIT foundation | ADR-002/004/005/007, DB contract | Derived AC/TEST IDs + verifier |
| Minimal shared UI | `3.1–3.10`, `3.12` | Interaction inputs | Frozen `Mx*`, session/editor/picker specs | ADR-002/006/007 | Widget/golden/a11y |
| Language Pair | `5.1.1–5.1.3` | `business/language-pair/**` | `languages`, first-run | ADR-006/007, schema v1 | Domain through E2E |
| Deck | `5.2.1–5.2.6` | `business/deck/**` | Library/create/empty Deck | ADR-001, schema v1 | Decision + transaction + E2E |
| Flashcards | `5.3.1–5.3.4` | `business/flashcard/**` | editor/list | ADR-001/007, schema v1 | Rollback/idempotency/E2E |
| Progress/SRS | `5.4.1–5.4.5` | `business/learning-progress/**` | settings/session/result states | ADR-003, canonical domain policy path | SRS rows/property/time tests |
| Six strategies | `5.5.1–5.5.6` | `business/study-mode/**` | five learning-mode specs + SRS Binary Review | ADR-003/007 + mandatory Factory | Shared factory/strategy contract suite |
| Session/start/recovery | `5.6.1–5.6.14` | `business/study-session/**` | picker/session/result | ADR-003/005/006/007, schema v1 | Durable attempt + fake-clock/restart E2E |
| Today/release | `5.7.1–5.7.5` | `business/today-dashboard/**` | dashboard | All accepted ADRs | Full Tier-1 first-learning gate |

## Current status rule

The item-specific table is authoritative for current Ready/Done status. Every other
item inherits `Blocked` from its longest matching prefix until an explicit Ready
or Done override satisfies the schema. Accepted ADRs alone never promote status,
and contradictory business/design prose must be reconciled before an override.

**Phase 0 precedence (owner, 2026-07-19):** no UI work package is promoted to
`Ready` while `P0.6` is open. This overrides dependency-order promotion — an item
whose dependencies are all Done is still not Ready if it changes a screen,
dialog, bottom sheet or UI state. Non-UI domain/data/policy rows are unaffected.
A UI item is additionally not Done without the visual-parity evidence required by
WBS §6.5 for every `MX-VIS-*` state it touches.
