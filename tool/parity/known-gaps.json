{
  "$comment": [
    "State-theme runs that are measured but cannot currently reach the <=3% gate,",
    "each with the reason it is blocked and the row that owns it. This file is a",
    "ratchet, not an excuse list: `gate.mjs` fails on any failure NOT listed here,",
    "and equally on any entry here that has started passing, so the list can only",
    "shrink. Every entry must also appear in the MX-VIS register in",
    "docs/wbs/memox-v6-development-wbs.md with the same reason."
  ],
  "gaps": {
    "MX-VIS-036--light": "Kit self-contradiction: SubdeckList.jsx renders crumbs + FilterRow + list, the shot draws a DECKS section and no filter chips. Owner rulings 2026-07-25 (JSX is the source of truth) and 2026-07-26 (the shot will not be regenerated) make this a permanent recorded gap, not a parked one. The app follows the JSX; this entry exists so the ratchet still blocks every other regression.",
    "MX-VIS-036--dark": "Same as MX-VIS-036--light.",
    "MX-VIS-037--light": "Same kit self-contradiction, loading branch: the shot is a skeleton of chrome the JSX never renders. Permanent per the 2026-07-26 ruling.",
    "MX-VIS-037--dark": "Same as MX-VIS-037--light.",
    "MX-VIS-069--light": "Measured: 7.67% light / 9.45% dark (8.35 / 10.43 before the create FAB landed). Every section of the kit composition is built and correct; the residual is four things that cannot be built here. (1) The strip carries 2 of the kit's 4 stats — minutes-studied has no producer at all (`int-9`) and words-learned-today is recorded only when a daily goal exists (`int-12`); the two missing cells shorten the strip by one row and shift everything below it up, which is most of the diff. (2) The app bar's notification bell needs a notifications feature and (3) its avatar an account, both WBS 14.x. Recorded rather than parked: the number is real and the causes are enumerated, so the ratchet blocks any regression while the row stays honest about why it is not green.",
    "MX-VIS-069--dark": "Same as MX-VIS-069--light.",
    "MX-VIS-078--light": "Measured: 24.88% light / 25.29% dark. Same kit self-contradiction the owner ruled on for MX-VIS-036/037 (2026-07-25 JSX is the standard; 2026-07-26 the shots will not be regenerated), one screen over. `ReviewMode.jsx` gives a study screen a nested app bar - back arrow, mode title, `format_size` and `more_vert` - with the progress header as the body's first child, which is what this build renders. The `study-session--*` shots draw a different shell: an X, the progress inline in the bar, one overflow. The app follows the JSX. Proved by measurement rather than assumed: seeding the shot's own scenario (25 cards, the 21st, prompting `school`) moved it 24.50 -> 24.88, so the counter and the prompt were never the residual. What is left that is buildable is the two app-bar actions the JSX names, and both are product-undefined (int-5).",
    "MX-VIS-078--dark": "Same as MX-VIS-078--light.",
    "MX-VIS-079--light": "Measured: 28.59% light / 28.96% dark. Two causes, both recorded rather than parked. (1) The same shots-versus-JSX contradiction as MX-VIS-078: this build renders the shell `ReviewMode.jsx` describes and the shot draws another. (2) The copy decides the layout here: exit-study-session.md §4 writes `Keep studying` / `Save and exit`, which say that leaving *saves*; the kit's shot writes `Stay` / `Leave`, which do not. The app's pair does not fit the kit's side-by-side halves at 390dp, so it wraps and the dialog grows a row taller than the shot. Clipping them to `Keep s...` / `Save a...` was the alternative and was worse. (2) is an owner ruling: adopt the kit's shorter labels, or keep the spec's and accept the taller dialog.",
    "MX-VIS-079--dark": "Same as MX-VIS-079--light.",
    "MX-VIS-084--dark": "Measured 2026-07-28 on first registration (`int-105`): 1.95% light / 3.20% dark. The screen had no row at all until this round, so this is a first number rather than a regression. The residual is not composition: the diff is dominated by fixture-versus-shot content — the kit's shot lists three Korean-term results where the fixture's query matches one English card — plus a trailing element on each kit row the app does not draw. Light passes at the same composition, which is what says the gap is content and one missing row affordance rather than layout. Recorded so the ratchet blocks any regression while the row stays honest about why it is not green; closing it needs a fixture shaped like the shot and a decision on the trailing element."
  }
}
